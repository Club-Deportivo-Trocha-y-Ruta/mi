# Technical Implementation Notes — Full History

> Detailed changelog-style technical notes for the Club Trocha y Ruta backend/frontend.
> Moved out of `CLAUDE.md` (2026-07-22) to keep the always-loaded project memory lean.
> `CLAUDE.md` keeps only the short evergreen gotchas; the full dated entries live here.

- `RPE_LABELS` in `RubricSliders.tsx` uses the validated OMNI 0–10 mapping (Reposo→Máximo, "Moderado" at index 5 = midpoint); frontend-only refactor, no backend/schema/migration change (2026-06-05)
- Session create/edit is a multi-step wizard (`components/training/session-wizard/`, feature 005). `session_kind`/`objectives` are now persisted end-to-end (were silently dropped by `TrainingSessionCreate/Update` + `create_session`); no migration (columns existed since `d4e5f6a7b8c9`). Drafts autosave to `localStorage` key `tyr:session-draft:v1:{userId}:{new|<id>}`, cleared on save/discard. Route file auto-uploads to the existing `/route-file` endpoint after create. See `docs/09-training-planning/session-wizard.md` (2026-06-07)
- Event-scoped chat fix (feature 010, 2026-06-10): when the chat opens from a competition, scoped tool variants now **omit** `season`/`valida_num` from their schemas — the previously REQUIRED params contradicted the system prompt and made the LLM ask "¿a qué válida te refieres?" despite knowing the event. New group tool `obtener_resultados_evento()` (pseudonyms only, never real names) answers group questions ("¿cómo estuvieron los muchachos?"); `race_chat_v1.md` gained a hard "NUNCA preguntes" rule + missing-data guidance; `_SessionStore` preserves the SystemMessage when truncating long sessions. Backend-only, no migration
- `sequence_number=99` convention retired (feature 014, 2026-06-15): the legacy workaround that assigned round number 99 to the Departmental Championship so it would be treated as a championship is replaced by an explicit `race_series.kind` enum (`cup` | `championship`). Each championship is now its own standalone series; `sequence_number` for championships is fixed to `1` server-side and never shown in the UI. Migration `b1c2d3e4f5a6` reclassifies the existing event. Do not use `sequence_number=99` in new code.
- Prefill import from competition (feature 015, 2026-06-16): `ImportWizard` gained an optional `raceEventId?: number` prop. When present (route `/competitions/{id}/import`), the new hook `useImportPrefill(raceEventId)` composes `useRaceEvent` + `useRaceSeriesList` (resolving the series by `event.series_id` — there is no `GET /race-series/{id}`) into an `ImportPrefill` view-model with status `loading | ready | blocked | error`. On `ready` it `reset()`s RHF with the stored values and renders identity (name/date/city/series/type/round) as a **locked read-only summary** (static text + `Lock`/`Pencil`, not `disabled` inputs — WCAG); type/series are derived (no in-flow control); `válida #` is hidden for championships (`series_kind` from `series.kind`). On `blocked` (series unresolvable) the import is stopped with an "Editar metadata" escape hatch → `/competitions/{id}/edit` (FR-009). Standalone `/competitions/import` is untouched — `useRaceSeriesList` now takes `{ enabled }` so the no-id path fires zero new requests (FR-007). **Frontend-only, no backend/migration**: the existing `/parse`→`/dry-run`→`/commit` pipeline links to the same competition because prefilled values equal the stored `(series_id, sequence_number)`. Mutation gate (`stryker.config.json`) extended to cover `useImportPrefill.ts`.
- Per-athlete AI launch in Insights cards (feature 010 extension, 2026-06-16): the per-athlete launch (US4) now lives **both** on `ResultsTable` rows and on each `InsightCard` of the Insights tab. Button extracted to shared `frontend/src/components/competitions/insights/AnalyzeAthleteButton.tsx` (props `label`, `alwaysShowLabel`, `showInsightsLink`) — `ResultsTable` consumes it with defaults (no behavior change); Insights cards pass `label="Analizar con IA"`/`"Re-analizar"`, `alwaysShowLabel`, `showInsightsLink={false}` ("Análisis iniciado" instead of the Insights link, since we're already there). `InsightsTab` receives `season`/`validaNum` as **props** from `CompetitionDetailPage` (`event.event_date` year + `event.sequence_number`) — kept as props, not an internal `useRaceEvent`, so the grid stays presentational/testable without a QueryClient. Button gated on coach/admin + `athlete_id > 0` (masked parent cards excluded) + season/validaNum present; rendered as a **sibling** of the clickable card (avoids axe nested-interactive). Freshness per item: `insight_id===null → undefined` (direct launch), `stale_run_id==null → null` (confirm modal), `string → stale` (direct). `useLaunchAthleteAnalysis` invalidation extended to also drop `club-insights-by-race` (grid + ResultsTable freshness) so the new run is reflected on completion. Agentic `valida_nums` contract unchanged (same as ResultsTable; championships resolve by `sequence_number`, out of scope per feature 016). Tests: `InsightsTabAnalyze.test.tsx`. **Frontend-only, no migration.**
- Race-analysis championship charts fix (feature 016, 2026-06-16): Distribution and Evolution charts now identify races by stable `event_id` (retired `valida_num`-based identity). New `GET /api/athletes/{id}/race-analysis/races?season=` endpoint returns a participation list with server-built labels via pure helper `build_race_label` (`backend/app/services/race/race_labels.py`). Distribution 500→404 for non-participated events (raises `AthleteDidNotParticipate`), 200 for DNF/small-field (deleted invalid empty fallback). Evolution championship point now keyed by `event_id` with `series_kind`+`label` and `ORDER BY event_date ASC` (was `valida_num, event_date` — mis-ordered championship). Frontend: `useAthleteRaces` hook + `raceOptionLabel.ts` helpers feed the picker; `DistributionChart` queries by `event_id`; `EvolutionChart` labels championship point distinctly. **No migration** — reuses feature-014 columns. Out of scope and untouched: AI insight text/chat, results ingestion, ranking, ComparatorPanel, agentic `valida_num` contract.
- AI provider switch to Anthropic for `app/services/ai/` (2026-07-10): `AnthropicProvider` (`app/services/ai/providers/anthropic_provider.py`) was already built and tested — this only flips defaults/env and fixes one latent bug. `Settings.ai_provider`/`ai_model` defaults changed to `"anthropic"`/`"claude-sonnet-5"`; `AnthropicProvider.complete()` no longer sends `temperature` to the SDK (`claude-sonnet-5` and the rest of the 4.6+ family return 400 on any non-default `temperature`/`top_p`/`top_k` — the constructor still accepts it for interface parity with `GoogleProvider`, just doesn't forward it). **`race/agents/` (analyst/critic/chat, LangGraph-orchestrated) intentionally stays on Gemini** — out of scope for this change, and it was previously hardcoded to read the *same* `AI_MODEL`/`AI_API_KEY` settings regardless of `AI_PROVIDER`, which would have broken it the moment those pointed at Anthropic. Decoupled via two new dedicated settings, `race_ai_model` (default `gemini-2.5-flash-lite`) and `race_ai_api_key`, consumed only by `build_chat_llm()` in `app/services/race/agents/_llm.py`; new env vars `RACE_AI_MODEL`/`RACE_AI_API_KEY` in `.env`/`.env.example`/Render. `GoogleProvider` and `google-genai`/`langchain-google-genai` remain in the codebase (factory `_PROVIDERS["google"]` still works) — not removed, since the race pipeline still depends on the latter. Backend-only, no migration.
- RAG subsystem fully removed (2026-07-10): the ChromaDB + Gemini-embeddings grounding layer over `docs/01-marco-teorico.md` was confirmed dead (never indexed — `data/chroma` was an empty dir, `retrieve_principles` node always returned `[]` via a silent `except Exception` fail-open) and has been deleted entirely: `app/services/race/rag/` (indexer/retriever/tools), the `retrieve_principles` LangGraph node + its graph wiring (`compute_metrics → recall_memory` now direct), `RaceAnalystState.principles`, `AnalysisInput.principles_citations`/`Citation` (schemas.py), the `consultar_marco_teorico` chat tool, and the `chromadb` dependency (including its `CHROMA_PATH` env var + bind mount in `docker-compose.yml`). `AnalysisOutput.citations_used`/`ChatResponse.citations_used` fields were **kept** (general-purpose `[n]`-scraping machinery, not RAG-specific) but now always resolve to `[]` since nothing populates citations anymore — analyst/critic prompts (`race_analyst_v1.md`, `race_analyst_v2.md`, `race_critic_v1.md`, `race_critic_v2.md`, `race_chat_v1.md`) had their citation-enforcement rules ("Cita siempre", critic penalties for missing/invented citations) removed accordingly, and the 10 golden eval cases (`evals/race_analyst/golden/case_*.json`) had `must_cite` flipped to `false` + `principles_citations` stripped (a `must_cite=true` case can never pass again with citations permanently empty). The `athlete_ai_insight.principles_cited_json` DB column was **kept by explicit decision** (persists `[]` always) — no Alembic migration. Also fixed a latent bug from the prior AI-provider-switch entry above: `tests/evals/test_race_analyst_eval.py`'s skip guard was still gating on `AI_API_KEY` (now Anthropic) for a test that calls Gemini — switched to `RACE_AI_API_KEY`. Backend-only.
- race/agents/ LLM provider made configurable (2026-07-10): `app/services/race/agents/_llm.py::build_chat_llm` is now a Factory (same Strategy+Factory pattern as `app/services/ai/factory.py`) dispatching on the new `Settings.race_ai_provider` (`"anthropic"` | `"google"`, validated) via a `_LLM_BUILDERS` dict — `_build_anthropic_llm` (lazy `langchain_anthropic.ChatAnthropic`, new dep `langchain-anthropic>=0.3.0,<2.0`; deliberately never sends `temperature` — same 400-on-non-default-sampling-params issue as `AnthropicProvider`) and `_build_google_llm` (existing `ChatGoogleGenerativeAI` logic, unchanged behavior). `Settings.race_ai_model` default changed from a hardcoded Gemini string to `""` (empty → resolves to `DEFAULT_MODEL_BY_PROVIDER[provider]`, `claude-sonnet-5` for anthropic); `race_ai_provider` defaults to `"anthropic"`. `_resolve_race_api_key()` lets `RACE_AI_API_KEY` fall back to `AI_API_KEY` when `RACE_AI_PROVIDER == AI_PROVIDER` (avoids duplicating the same key across both config blocks). `pricing.py::compute_cost_usd` now takes a required `provider` kwarg and looks up `_PRICING_USD_PER_1M[provider]` (`anthropic`: 3.00/15.00 per 1M, `google`: 0.075/0.30 per 1M — was a single Gemini-only constant pair before); `call_llm()` in `_llm.py` gained an optional `provider` kwarg (defaults to `Settings.race_ai_provider`) threaded into the cost calc — analyst/critic/judge call sites unchanged (`call_llm(llm, prompt)`). Local `.env` deliberately left on `RACE_AI_PROVIDER=google` (has a working Gemini key already; flip to `anthropic` once a real `AI_API_KEY` is set). Backend-only, no migration; `google-genai`/`langchain-google-genai`/`GoogleProvider` remain available (provider stays switchable, nothing hardcoded either direction).
- Interval block duration usability (feature 034, 2026-07-22): interval structure/template blocks gain a `duration_type` discriminator (`fixed` | `open_lap`), `duration_s` made nullable — `fixed` keeps today's exact-seconds behavior, `open_lap` carries no duration and is restricted to warmup/cooldown block types, never allowed inside a repeat group (enforced both in the editor and server-side at save time). Plan-vs-actual comparison `ENGINE_VERSION` bumps 1→2 with a new informational row status `libre` (open block, lap consumed, actual duration shown, never `fuera_tolerancia`); comparisons stored under engine v1 render verbatim, no retroactive recomputation. New frontend `MmSsInput` component replaces raw-seconds entry for fixed blocks in both the structure editor and the template library editor — storage stays integer seconds, only display/entry changes to minutes:seconds. PDF instructivo (all brands) renders open-ended blocks as "Libre — hasta botón de vuelta" (zone/cadence still shown) in place of a fixed time. Migration `c7d8e9f0a1b2`. Backward-compatible/additive — pre-existing rows and drafts without an explicit duration type default to `fixed`.
- Sessions module restricted to training only (2026-08-20): the "Tipo de sesión" selector was removed from the session wizard (`StepGeneral`, `StepReview`) and `SESSION_KIND_OPTIONS` deleted from `trainingSession.schema.ts` — every session created from the UI is now `entrenamiento`, and `mapDraftToFormValues` (`sessionAssistant.schema.ts`) hard-codes that value so the AI assistant can no longer pick another kind unseen. **UI-only: no migration, no API change** — the `session_kind` column/enum and the `TrainingSessionCreate/Update` field stay as they are, so historical sessions keep their value (editing one preserves it). Outings and joint activities move to the **Calendar** (`club_event` → "Actividad conjunta", `group_training` → "Salida"). Consequently `get_conjoint_sessions` (`services/training/reports.py`) now unions calendar events + legacy sessions, ordered by date, skipping cancelled events and events already linked to a session (`TrainingSession.calendar_event_id`), and each row carries a new `kind_label`; both report templates render `s.kind_label` with a fallback to the old `s.kind` expression (DOCX template regenerated via `scripts/generate_docx_template_monthly_report.py`). `_derive_photo_section` was deliberately left alone: new session photos land in "Grupo de Alto Rendimiento" (or "Competencia" by race-date heuristic), since calendar activities have no session media. Tests: `backend/tests/test_report_conjoint_sessions.py`. The `/activities` tab (Strava review) is unrelated and untouched.
- AI Insights Tab Review, feature 036, all 5 waves closed (2026-09-01): see `docs/implementation-status.md`'s own entry for the full per-wave breakdown; this note only records the cross-cutting technical facts. **Provider**: production race AI stays on `google`/`gemini-3.1-flash-lite` (D1) — `spec.md`/`research.md` wrongly assumed an Anthropic migration; only `config.py` defaults and the CI eval (`race-eval.yml`) were realigned, no provider-factory code changed. **Identity/labels**: `AthleteInsightOut` and the TS type gained `event_id`/`event_date`/`series_kind`; `insights_history.list_athlete_insights` now does a `LEFT JOIN` + `contains_eager` ordering by `event_date DESC` server-side (season-aggregate rows, `valida_num=0`/`event_id=NULL`, sort last); one surviving label helper, `validaLabel` (`lib/insights.ts`, roman numerals) — `lib/raceCalendar.ts`'s arabic-numeral `getValidaLabel` is gone. **Concurrency guards**: `POST .../runs` 409s on a second active run for the same athlete+válida (reuses `services/race/group_launch.find_active_run`); `POST .../season-summary`'s dedup lock commits before the LLM call, so a double submit is 409 not a post-spend 500. **`is_fallback` plumbing**: migration `463c1f0ccb38`; both `_insight_to_out()` and `create_season_summary()` now set it (neither did before); enforced double — client hides the checkbox/button, `POST attach-insights` 422s server-side if a fallback id is sent anyway. **Privacy fix, out-of-band**: `forbidden_names` in the season-summary guardrail was always `[]` in production (`UserModel.full_name` doesn't exist; the `AttributeError` was swallowed by a bare `except` logging a warning) — rebuilt from `first_name`/`last_name`, `except` narrowed to `SQLAlchemyError`. **Bundle**: the tab is lazy-loaded from both `AthleteDetailPage.tsx` and `MyAthleteDetailPage.tsx` (main chunk −92 kB raw/−24.8 kB gzip). **e2e environment fix**: `frontend/.env.local`'s `VITE_API_BASE_URL=` (empty string) defeated `??`'s null/undefined-only fallback in `src/api/client.ts`, leaving every e2e spec's `:8000`-only `page.route()` predicate matching nothing — fixed by pinning `VITE_API_BASE_URL` in `playwright.config.ts`'s `webServer.env`, not touching the user's local dev config; this was previously misdiagnosed as "Playwright route interception doesn't work in this sandbox," which was never true. **Touch targets**: `ComparatorPanel.tsx`'s shared `TAP_TARGET_CLASSES` constant was `min-h-[44px]` (the shared `Button` primitive's own "default"/"icon" sizes are also 44px, short of this project's 48px floor) — bumped to `48px` (also fixing its swap button's `min-w-[44px]` → `min-w-[48px]`), plus 13 more standalone `min-h-12` overrides across `HeroLastInsightCard.tsx`, `SeasonSummaryButton.tsx`, `LaunchAnalysisForm.tsx`, `DistributionChart.tsx`, and the newsletter action bar + comparator-sheet trigger in `AthleteAIAnalysisTab.tsx` (17 distinct violating controls fixed in total). Left alone as pre-existing/shared, confirmed unchanged by this feature via `git diff`: `AppShell`'s `SidebarNav.tsx` (44px links, 28–44px buttons, rendered on every authenticated page), `AthleteDetailPage.tsx`'s own top-of-page chrome (tab strip, "Volver a lista", "Enviar informe" — shared by every tab, not specific to AI Insights), and `components/ui/sheet.tsx`'s 28×28 close button (shared by 6+ unrelated features' side panels). **Still red**: `evals/race_analyst` golden gate at composite 0.651 against a 0.75 blocking threshold (T059) — a real, unresolved regression in analysis quality, not a test-infra problem; needs its own prompt-iteration budget.
- AI Insights v3, Waves 1-2 (feature 037, causal/field-relative/prescriptive analysis, 2026-09-02): full architecture and contract in `docs/10-race-results/spec-insights-v3.md`; this note only records the bug fixes and cross-cutting facts. **Five bugs fixed** (root causes of the coach's "muy básicos" feedback, `spec.md` §Problem statement): (1) `_compacted_season_record` read `sequence_number` from `RaceResult` instead of `RaceEvent` → `valida_num` was `None` on every historic row → the critic's ground-truth lookup found nothing → `must_block=true` on every insight ("Confianza baja" globally) — fixed by populating `valida_num` via `events_by_id`. (2) `_REC_BULLET_RE` rejected recommendation bullets ending in `.` → `recommendations_json=[]` on every insight — relaxed (also tolerates optional `horizonte=…`/`catálogo=…` trailers); moot for v3 output (actions are typed, never regex-parsed, see below) but still governs the v2 rollback path. (3) The season summary reused the per-válida prompt with `valida_num=0` and no season table, ran synchronously with no critic/HITL, and hardcoded its model — now runs through the same LangGraph pipeline as a válida (`analysis_kind="season"`, dedicated `race_season_summary_v3.md` prompt, critic + HITL, model resolved per role). (4) Gender was hardcoded to "la deportista" for every athlete — `athlete_ref` ("el deportista"/"la deportista") is now derived from `Athlete.sex` and injected into both v2 and v3 prompt contexts. (5) `race_ai_model` was a single hardcoded string shared by both agent roles — `build_chat_llm(role="analyst"|"critic"|"chat")` now resolves `RACE_AI_ANALYST_MODEL`/`RACE_AI_CRITIC_MODEL` independently (empty → falls back to legacy `RACE_AI_MODEL`, then to the per-provider default). **Contract change**: analyst output is no longer 3-section markdown re-parsed with regex — it is a structured `InsightV3` JSON (Gemini structured output, `app/services/race/insight_v3.py`) that generates markdown deterministically for `summary_text`/newsletter/chat, never the reverse. **New data sources reach the model**: `load_athlete_context` node adds anthropometry (maturity offset, months from PHV, growth velocity — **never** weight/BMI/nutrition), a 28-day training window (attendance %, RPE, rubric means, technical foci, scrubbed coach feedback), and the club's technique/strength/interval catalog; `compute_metrics` adds `field_context` (percentile, expected-vs-actual position, gap to P1/P3/median, field strength) via new pure-pandas `field_metrics.py`. **New DB columns** (migration `ai_insights_v3_columns`, down-revision `463c1f0ccb38`): `structured_json`, `coach_answer_text`, `coach_answer_at`, `coach_rating` on `athlete_ai_insights`. **New endpoint**: `POST /api/athletes/{id}/race-analysis/insights/{insight_id}/answer` (coach/admin only, parent 403) persists the coach's inline answer + 👍/👎 rating, scrubbed, and it feeds `coach_dialogue` (last 3 approved) into the next run's prompt via `recall_memory`. **Rollback**: `RACE_AI_PROMPT_VERSION=race_analyst_v2` reverts per-válida launches to the v2 pipeline without a deploy (season summary has no v2 equivalent to roll back to — v2's "season summary" was never a dedicated analysis). Backend: migration + new modules (`insight_v3.py`, `field_metrics.py`, `ai/athlete_context.py`, `ai/prechecks.py`) + 3 new prompts; no removal of v2 modules. Wave 3 (UI: `InsightV3Card`, coach-answer form, athlete-scoped chat panel) and Wave 4 (golden eval v3, privacy audit, this doc pass) tracked separately in `specs/037-ai-insights-v3-causal/tasks.md`.
- AI Insights v3, Waves 3-4 + SC-1 verification (feature 037, 2026-09-02): UI (`InsightV3Card` with field-reading chips, evidence pills, catalog-linked actions and the coach-question block; `CoachAnswerForm`; athlete-scoped `AthleteAnalystChatPanel` sharing `useChatSession` with the competitions chat; season summary now shows the run timeline/HITL), golden eval v3 (`evals/race_analyst/golden_v3/`, `eval/scorer_v3.py`, `judge_v2.md`, `RACE_EVAL_VERSION` default `v3`), privacy audit PASS on all six points, 451 consent gate extended to `start_athlete_run`, plural `structured_drafts` in the HITL payload. **Four defects found only by regenerating a real athlete against Gemini** (`backend/scripts/regenerate_athlete_insights_v3.py`): (1) the 30 s `ai_timeout_seconds` also bounded the v3 analyst and `gemini-3.8-flash` needs 40–60 s per válida → new `RACE_AI_V3_TIMEOUT_SECONDS` (default 120); (2) `load_anthro_context` returned `None` when the only anthropometric record was measured after the race while the critic's ground truth used the undated latest record → contradiction and `medium` confidence on a correct insight; the loader now falls back to the closest later record flagged `measured_after_event` and the v3 ground truth reads `anthro_context`; (3) `grounding_numbers` came from the whole rendered prompt, so the few-shot example's figures counted as evidence → `grounding_source_text` cuts at `# Ejemplo resuelto`; (4) prechecks never filled `catalog_ref.label` (UI showed "H" instead of the skill name). Prompts v3 also gained a tone rule (no "a muerte"-style wording about a minor's effort). Regression tests in `tests/services/race/ai/test_sc1_regressions_037.py`.
- Bitácora de etapa — newsletter redesign, feature 038, all 4 waves + integration pass (2026-09-02): full breakdown in `docs/06-parents/038-bitacora.md` and `docs/implementation-status.md`'s own entry; this note only records cross-cutting technical facts from the integration/verification pass (T404/T405), run after 4 waves built in parallel by agents with no visibility into each other's work. **Zero integration bugs found** across the five highest-risk seams named for verification: `athlete_monthly_newsletters.py`'s two `build_stage_log(...)` call sites (initial generation + `_rederive_stage_log` after PATCH) both match `stage_log_builder.py`'s positional signature exactly; `athlete_monthly_newsletter_v2.py` imports `extract_numeric_tokens` from `app.services.race.insight_v3` correctly; `NewsletterDeliveryEvent`/`DeliveryEventType` names are consistent across the model, `parent_newsletters.py`, `webhooks_resend.py`, `newsletter_dispatcher.py`, and the coach router's delivery panel; `StageLogView` exports exactly the name/props (`StageLogView`, `StageLogViewProps`) both `DevicePreview.tsx` (studio) and `ParentNewsletterPage.tsx` consume; MSW (`stageLogHandlers.ts`) covers every new coach and parent endpoint including the four error-variant handlers (409/451/503 on regenerate, 409 on convert, 404/403 on parent routes). **Migration heads**: `6b998c214e5a` (`down_revision="f7a8b9c0d1e3"`) is the only new head — `main` already carried two pre-existing, unrelated heads (`a1b2c3d4e5f8` password-reset-tokens off a different chain, and `f7a8b9c0d1e3`) before this feature, so the post-038 head count (2) is unchanged, not a new fork introduced by parallel migration authors. **Test suite reconciliation**: backend `pytest -q` — 206 failed + 5 errors / 4098 passed (vs. the pre-branch baseline's 196 failed / 3917 passed — the delta is entirely new tests added by this feature's own waves, not regressions). Every failure was individually attributed to one of four buckets, with zero left unexplained: 187 `OperationalError` (`Can't connect to MySQL server on 'mysql'` — no local MySQL, pre-existing and unrelated to this feature), 17 WeasyPrint `libgobject-2.0-0` missing-native-lib failures (6 in the pre-existing `test_newsletter_pdf_layout.py`, 10 in this feature's new `test_stage_log_pdf.py`, 1 in `test_athlete_monthly_newsletters_router.py::TestLegacySnapshotBackwardCompat` — same cause, expected on this Mac without `brew install`), and 7 pre-existing bugs confirmed via `git diff main` to be in files this feature never touched: 3 already known from feature 036's close note (`test_ai_factory.py`, `test_calendar_audiences.py`, `test_calendar_models.py`) plus 4 newly attributed here (`test_training_session_notifications.py`, all 4 asserting `notification_service.send` was called) — root cause traced to `TaskDispatcher.dispatch()` (`app/services/notification/task_dispatcher.py`, untouched by 038): when a `background_tasks=None` dispatcher runs inside an already-running event loop (pytest-asyncio's case), it does `loop.create_task(...)` and returns immediately without awaiting, so the assertion runs before the scheduled coroutine executes — a latent, pre-existing bug in fire-and-forget scheduling, not a regression from this feature. `ruff check` clean on every 038-authored/038-touched file (a handful of pre-existing `F401`/`F841` findings in `athlete_monthly_newsletters.py`, `parent_athletes.py`, `newsletter_builder.py` were confirmed via `git diff` to be outside this feature's changed lines, left alone). Frontend: `tsc --noEmit` clean; `vitest run` — 1 failed / 4217 passed, exactly the pre-existing `datetime.test.ts` timezone assumption, zero 038-caused frontend failures. **Gaps carried forward, not implemented in this pass**: T403 (Playwright e2e coach-studio→send→parent-read flow, Fast-3G LCP check, Gmail-Android email checklist) has no artifacts in the working tree; SC-1 (regenerate June 2026 for 6 athletes on the local DB) needs a live MySQL instance, not run in this environment.
- Season comparison groups, feature 039 (2026-09-03): the "Evolución en la temporada" charts (family newsletter, athlete detail, AI insights) stop mixing Copa Valle rounds with the Departmental/National Championships — a championship races a different field, so its position and gap were never comparable with a cup round on the same line. Full per-step breakdown in `docs/implementation-status.md`'s own entry; this note records the cross-cutting technical facts. **Derived comparison group, no migration**: new pure module `app/services/race/comparison_groups.py` (`build_comparison_group`, `group_label`, `split_progression`) groups by `race_series.kind`/`id`/`level`, columns already present since feature 014 — a cup is one group with all its rounds (ordered by earliest raced round), a championship is its own single-race group. **`GET /evolution` additive contract**: new optional `series_id` query param restricts `series` to one group; response gains `groups[]` (every comparison group the athlete raced, cups first then championships by date) and `selected_group` (echo of the applied filter, `null` when omitted); each `EvolutionPoint` gains `series_id`/`series_name`/`series_level`/`comparison_group`/`field_size`/`percentile`. Every pre-existing field keeps its name, type and semantics — clients that ignore `groups` keep working exactly as before (`contracts/evolution-api.md`). **Newsletter snapshot keys** (`athlete_newsletters.metrics_snapshot`, additive): `email_blocks.race_results` gains `cups[]` (`{series_id, label, history[]}`, deduped by `event_id` across linked competitor rows) and `championships[]` (`ChampionshipReading`: position, field size, gap %, percentile, `finished` flag); `pdf_only_blocks.charts_context` gains `cups[]` with per-cup `positions`/`gap_pcts`/`points_accumulated`. Snapshots stored before this feature lack both keys — the PDF template treats a missing key as an empty list, no error, no regeneration required to keep rendering. **Template restructuring**: the season-evolution section of `templates/documents/pdf/athlete_stage_log.html` (feature 038's stage-log template) was moved out of the anthropometry annex — before this feature it only rendered when a growth record fell inside the reporting month; now it is its own independent page, gated on `cups or championships` from the season, one heading `Evolución en la {cup label}` per cup plus a `Campeonatos` section from the new `charts/championship_card.html.jinja` stat-tile partial. **AI pipeline**: `RaceAnalysisState.metrics.progression_groups` (`{"cups": {series_id: [rows]}, "championships": [rows]}`) is now computed alongside the existing flat `progression`; race resolution is anchored by `event_id` via new `_find_analyzed_record` in `compute_metrics.py` — never by `valida_num` alone, which used to collide a championship with cup round 1 (the same bug class feature 016 fixed for the Distribution/Evolution charts, now closed for the AI pipeline too); `_compute_season_comparative` restricts its priors to earlier rounds of the *same* `series_id`, ordered by date, and returns `([], "first_reference")` for a championship; the old `_event_label` helper was deleted in favor of the shared `race_labels.build_race_label`. `race_analyst_v3.md` gained inviolable rule 10: a championship (departmental or national) is never compared by position or gap with a cup round, and is read by percentile/field size/strength instead; `race_analyst_v2.md` is untouched and remains a valid `RACE_AI_PROMPT_VERSION` rollback. New golden case `evals/race_analyst/golden_v3/case_009.json` covers a national-championship analysis. **Dedupe-by-`event_id` fix**: an athlete linked to more than one `RaceCompetitor` record (raced under different name spellings) could have the same race counted twice in the season progression; `newsletter_builder` now drops duplicates on `event_id` before splitting into groups, and the field-metrics lookup for championships tracks `seen_event_ids` for the same reason. **Operational notes**: the golden evaluation gate (`pytest -m golden`, threshold 0.75) is pending a re-run — today's run scored cases 001–002 (0.840/0.820) before hitting the Gemini free-tier quota (HTTP 429 on `gemini-3.8-flash`), so cases 003–009 fell back to the deterministic path and were not measured; the scoreboard file was restored to its pre-run version, the gate stays open. Backend commands in this environment must use `.venv/bin/python -m pytest` from `backend/` — the `pytest` shim on `PATH` is broken. Rendering a WeasyPrint PDF locally on macOS (not just installing `weasyprint`) additionally needs Pango via Homebrew and `DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib` set for the process, or PDF-render tests fail on missing native libs even though the Python import succeeds. **Review follow-ups** (post-integration-review pass, `checklists/integration-review.md` §D, F-1 and F-3 already closed before this pass): (F-4) `_progression_to_md` and `_v3_season_block` (`agents/analyst.py`) now render two headed markdown tables — `**Válidas de copa**` / `**Campeonatos (pelotón propio, no comparable con la copa)**` — whenever a season mixes both kinds, with `_progression_to_md` additionally sub-grouping cup rows by `series_id`/`series_name`; `_v3_season_block`'s rows (sourced from `field_context`) don't carry `series_id` yet, so its cup table still renders as a single group in production — a documented residual, not a regression. (F-5) `athlete_stage_log.html` gained a legacy-snapshot fallback: a pre-039 snapshot (flat `positions`/`gap_pcts`/`points_accumulated`, no `cups`) now still renders the old single "Evolución en la temporada" block instead of silently dropping the season page. (F-6) the three native selects in `EvolutionChart.tsx` (`#evo-season`, `#evo-metric`, `#evo-group`) gained `min-h-12`, meeting the 48px touch floor instead of the 36px parity exception. (F-7) `_build_progression_groups` (`compute_metrics.py`) now casts `progression_groups["cups"]` keys to `str`, matching `contracts/ai-context.md`'s `"<series_id>"` notation. (F-8) `build_evolution`'s `cat_stats` CTE now counts `field_size`/percentile over every `status='finished'` row regardless of a recorded time, aligning with `compute_field_metrics`'s criterion; a separate `cat_size_with_time` column keeps gating the ≥5 rule for the time-based percentile. (F-9) the "Campeonatos" block in `athlete_newsletter_pdf.py` now shares the same `has_race_this_month` gate as the cup charts (previously ungated) — a real behavior change, covered by a new regression test — and `docs/06-parents/003-newsletter-improvements.md` was corrected to state it. (F-10) `EvolutionPoint.series_name`/`RaceParticipationOption.series_name` now fall back to `"Serie"` instead of `""` when the column is `NULL`, avoiding a 500 on the `min_length=1` schema constraint. (F-2) `EvolutionChart.tsx`'s low-confidence notice now derives from the group actually displayed on first render (new `confidenceForPoints()` in `lib/insights.ts`, applied to `displaySeries`) instead of `query.data.confidence` (season-wide) until the user explicitly picks a `series_id`, at which point the server-filtered value is used as-is.
- Growth module redesign, feature 040 — close-out on the isolated e2e stack (2026-09-05): the items deferred on 2026-09-04 (T027, T028, T044/T054/T064 and the Playwright leg of T072) were closed after removing the blocker. **Fresh-database migration fix (cross-feature, pre-existing)**: three historical migrations imported catalog modules deleted by feature 038 at `upgrade()`-time (`e1f2a3b4c5d6_technique_gymkhana_library.py`, `f1a2b3c4d5e6_add_layout_json_to_technique_exercises.py`, `a7b8c9d0e1f2_strength_training_library.py`); each now wraps the import in `try/except ModuleNotFoundError` and returns early — the seeded rows are dropped by `d0e1f2a3b4c5` a few revisions later, so skipping the seed is behaviour-preserving for every database that replays history from empty (new Render instance, restored backup, CI, the e2e stack). Verified: full chain from an empty MySQL 8.4 volume, twice. **Isolated e2e stack is now the reference for Playwright**: `docker-compose.e2e.yml` + `frontend/scripts/e2e-stack.sh` (project `trocha-e2e`, ports 8001/3307/8026, synthetic dev seed, AI fake provider, Strava off); `frontend/playwright.config.ts` reads `E2E_APP_PORT`/`E2E_API_BASE_URL`; `npm run test:e2e:isolated`. Never point Playwright at compose project `me` (real club data). **Demo seed gains measurements**: `backend/scripts/seed.py::seed_demo_anthropometry` adds three synthetic anthropometry records to the demo athlete (dates before 2026-04-14 so the record created by `anthropometry.spec.ts` stays the latest and the velocity stays positive), computed with the same services as `POST /anthropometry`; guarded by the existing 'club already exists' early return, so the developer's stack is untouched. **e2e helpers**: `frontend/e2e/helpers/demo-athlete.ts` resolves the seeded athlete by API (the first with measurements) instead of clicking the first table row — `athletes.spec.ts` creates athletes in parallel and reorders the list; `frontend/e2e/helpers/session.ts::realTokens(role)` logs in through the API and injects **real** tokens for the ten specs that mock part of the API from an `addInitScript` session: the app shell (sidebar badges, dashboard summary, inbox) now calls six endpoints those specs never mocked, a fake token got 401 → `/auth/refresh` 401 → forced logout, and every assertion after that failed — this alone recovered ~40 tests that had been failing since features 033–039 added those shell queries. Stale assertions fixed on the way: the admin bottom bar no longer has 'Biblioteca' (retired in 038; 3 areas + Más), the newsletter heading is 'Bitácora de <mes> <año>', logout lives in the user menu (`user-menu-trigger` → menuitem). **Two product defects found by the e2e run and fixed**: (1) FR-016 — `AnthropometryHistory.tsx` in `mode="parent"` still showed the 'Offset', 'Estado PHV' and 'Edad PHV' columns, the PHV badge on the mobile cards and 'Maturity Offset'/'Edad al PHV'/'Estado'/training implications in the record dialog; all are now coach-only (`showClinical`), with regression tests. (2) R-02 — the family PDF drew an **empty** weight-for-age frame for athletes over 10 y ('Peso (kg) … Datos de referencia no disponibles'); `newsletter_builder._build_percentile_charts_block` now omits the weight chart when the builder reports `weight_over_10y`. **SC-002 measured**: on a 1024 × 768 landscape tablet the coach page's header (athlete card + T070 tiles + guardians + a two-row tab bar) ends at ≈ 610 px, so the growth status row is cut and the next-measurement card sits at ≈ 900 px — one short scroll; `growth.spec.ts` asserts the no-scroll criterion at 1024 × 1366 (iPad Pro portrait) and documents the landscape numbers. The top tiles and the tab's status row also repeat 'Etapa'/'Velocidad de talla' on the coach page when the growth tab is open — design debt, not changed here. **Pre-existing lane fix**: `tests/services/race/test_mysql_dialect.py` still called `build_distribution(valida_num=…)`, whose signature moved to `event_id=` in feature 039; `pytest -m mysql` is 7/7 again. **Suite-wide Playwright state** (isolated stack, idle machine): feature 040's own specs plus `auth`/`athletes` are green; the remaining red specs are outside this feature — `target-size.spec.ts` sweeps for ≥ 48 px while the feature-033 design system sets 44 px (`min-h-11`) on sidebar/list rows (owner decision: relax the sweep to 44 or grow the targets), and the real-data specs of features 014/015/025/032/036 (`competitions-unification`, `cup-vs-championship`, `prefill-import-from-competition`, `invitations`, `parents`, `session-content-unification`, `anthropometry-record-explanation`, `cold-start`) hard-code ids/names of the developer's database or need seed data (race series/events, invitations, sessions) that the demo seed does not have. Numbers per spec in `specs/040-growth-module-redesign/checklists/frontend-gate.md`.
- Stage-log PDF: emoji icons replaced by inline SVG, "Puntos acumulados" chart disabled (2026-09-04, coach feedback on `bitacora-2-2026-07.pdf`). **Tofu boxes**: the trail waypoints and the "Cima del mes"/"Brújula de la familia" headings used emoji entities (`&#128681;` 🚩, `&#127941;` 🏅, `&#128293;` 🔥, `&#11088;` ⭐, `&#128205;` 📍, `&#129517;` 🧭, `&#127956;` 🏔) and rendered as `□` in the PDF — WeasyPrint falls back to DejaVu Sans (`Font-face 'Inter Variable' cannot be loaded` on this box, and no emoji font is embedded anywhere), which has no glyphs outside the BMP. New macro `templates/documents/pdf/_icons.svg.j2::pdf_icon(name, size, color)` draws them as inline SVG (lucide geometry, viewBox 24×24, stroke-width 1.8, explicit `stroke` color — `currentColor` is not reliable in WeasyPrint's SVG engine), following the convention of the pre-existing but unused `templates/documents/pdf/icons/*.svg`. Consumed by `_trail.svg.j2` (waypoint icons, `pdf_icon(wp.icon)` with the same `dot` fallback the old `&#8226;` branch had) and by the two headings in `athlete_stage_log.html`. `templates/email/athlete_stage_log.html` keeps the emojis on purpose — the mail client supplies its own emoji font, so there is no tofu there. **"Puntos acumulados" chart**: commented out (not deleted) in both season blocks of `athlete_stage_log.html` (per-cup and the F-5 legacy-snapshot block) plus its macro import — the curve only ever goes up, so it does not separate a good month from a bad one for the family. The remaining two charts moved from three 33 % cells to two 50 % cells; `points_accumulated.svg.jinja`, `_build_charts_context`'s `points_accumulated` key and their tests are untouched, so re-enabling is a matter of uncommenting. **Page-count regression fixed in the same pass**: `test_full_month_with_annex_still_within_three_pages` was rendering 4 pages (reproduced on the pre-change tree, so it predates this work) — the body overflowed page 2 by a few dozen points, the "Nota del entrenador" landed alone on a near-empty page 3, and the annex's `page-break-before: always` opened page 4. Four changes, all scoped to this template: (1) a template-local `<style>` block tightens `h2`/`h3`/`p` margins and section spacing (16pt → 10pt) and overrides `@page` margins to 24mm (from `layout.html`'s 28mm — still ample for the running header and the legal footer, and `layout.html` itself is untouched because the other PDFs share it); (2) "Insignias del mes" and "Fotos del mes" became two independent `break-inside: avoid` blocks instead of one tall indivisible one that jumped whole and wasted half a page; (3) photo captions are clamped to a single line (`nowrap` + ellipsis) and thumbnails to 52pt — with the 8-photo `_PHOTO_SOFT_CAP` and narrow cells, a 200-char caption used to wrap 3-4 lines *per photo*; (4) as a last-resort valve, `generate_stage_log_pdf` now counts the rendered pages (`_pdf_page_count`, pdfplumber, already a prod dependency) and, if a month with an annex still exceeds `_MAX_STAGE_LOG_PAGES = 3`, re-renders once with the new `annex_page_break=False` context flag so the annex starts flush against the body instead of opening a page. The second pass only runs in that case; the typical month is settled by the first render. New regression test `test_heaviest_month_with_annex_still_within_three_pages` exercises the worst case the model admits (3 long observations, 8 photos, 6 badges, 5 effort weeks, coach note at the word limit). **Photo gallery redesign** (same day, coach feedback "las imágenes no se logran ver bien"): the section used to lay *every* photo of the month (up to `_PHOTO_SOFT_CAP` = 8) in a single table row, each keeping its own aspect ratio — thumbnails came out tiny, verticals and horizontals unaligned, and a 200-char caption wrapped over several lines per photo. It is now a single row of up to **4** photos at 25 % width with a fixed 100pt frame filled via `object-fit: cover` + `object-position: center 35%` (high framing so faces are not cropped), captions truncated at 48 chars, and an italic "Hay N fotos más del mes en el portal." line when the month has more — the parent portal (`ParentNewsletterPage` → `StageLogView` → `PhotosGrid`) shows all of them, the cap is the PDF's alone. The frame carries `overflow: hidden` + `font-size: 0` on purpose: when a thumbnail 404s, WeasyPrint paints the `alt` text in its place and it used to spill across neighbouring cells. Two supporting layout moves kept the 3-page budget with the bigger photos: the "Nota del entrenador" block now sits *before* the badges/gallery (it fills the tail of page 2 instead of being the block that overflows), and the annex's season charts went from 130pt to 112pt tall so the annex (533pt) fits under the gallery on page 3 when the escape valve drops the forced page break. Covered by `test_photo_gallery_caps_at_four_and_points_to_portal` and `test_photo_gallery_without_extras_has_no_portal_note`.
- Growth module redesign, feature 040, all five user stories + polish docs pass (2026-09-04): full per-phase breakdown in `docs/implementation-status.md`'s own entry; audit with `file:line` evidence in `docs/18-growth-module-redesign/proposal.md`. This note records the cross-cutting technical facts. **WHO switch, no dual reference left**: the backend persisted Z-scores/percentiles against CDC while the frontend curve/tooltip computed against WHO 2007 — now WHO 2007 is vendored and seeded (`app/data/who_lms/{who_height_for_age,who_bmi_for_age,who_weight_for_age}.csv`, 336/336/120 rows, deterministic exporter `scripts/export_who_lms_csv.py` with SHA-256-stable output and a provenance README) and `POST /anthropometry` computes and persists against it. **New additive column**: `anthropometric_records.growth_source` (nullable `Enum('WHO','CDC')`, migration `2a8baa967cc6`, no default, no backfill on migrate — existing rows round-trip `null` by design until recomputed). **Idempotent recompute**: `app/scripts/backfill_anthropometry.py::recompute_to_source(session, target=WHO)` re-derives Z/percentile/band for every historical row, nulls weight-for-age above 120.5 months (Res. 2465/2016 cutoff), is a no-op on a second run, and writes a band-change report to `./data/growth_band_changes_<YYYYMMDD>.json` carrying `athlete_id`/`record_id`/`indicator`/`previous`/`current` only — no name, no birth date (birth date is read in memory to compute age and never written out). **Growth-summary contract**: new `GET /api/athletes/{id}/growth-summary` (`app/services/growth_summary.py` — pure function, no DB access — + `app/routers/growth.py`, contract in `contracts/growth-summary-api.md`) answers the coach's decision inputs in one query, two rows (`ORDER BY evaluation_date DESC LIMIT 2`): growth velocity (cm/año, with an explicit "orientativo" flag when the measurement interval is under 30 days), months from PHV, next-measurement due date/status, and the latest height/BMI bands — the same helpers (`services/measurement_alerts.py`) the dashboard and the AI context builder already used, not reimplemented. Frontend consumes it via `useGrowthSummary` (TanStack Query, kept out of the persisted-cache allow-list) to feed the tab's top status row, replacing three near-empty longitudinal charts that used to open the tab. **Lazy growth chunk**: the tab and its WHO LMS JSON used to ship inside the entry bundle together with a statically-imported, module-preloaded recharts chunk. Both are now reached only through a dynamic `import()` from `GrowthTab.tsx`, lazy-loaded from both `AthleteDetailPage.tsx` and `MyAthleteDetailPage.tsx`. Measured in `checklists/build-baseline.md`: entry 336.68 → 283.38 kB gzip; new `GrowthTab-*.js` lazy chunk 17.42 kB gzip (budget 150 kB); `npm run check:chunks` green (recharts chunk and the WHO JSON no longer statically imported or module-preloaded from the entry); effective first-paint JS (entry + the one still-preloaded chunk) down 158.90 kB gzip against a ≥ 100 kB target. The entry route still exceeds the Constitution IV 250 kB budget (283.38 kB) — a pre-existing violation, reduced but not closed here; closing it needs a separate `App.tsx` route-splitting pass. **Removed components**: `GrowthCharts.tsx` (the three near-empty longitudinal charts) and the monolithic 875-line `PercentileCurves.tsx` are deleted, replaced by `GrowthCurveSection.tsx` / `PercentileChart.tsx` / `PercentileToolbar.tsx` / `PercentileTable.tsx` (a characterization-test suite pinned the old component's behavior before the split, then was folded into the new components' own tests); the page-local `StatCard` duplicated in both `AthleteDetailPage.tsx` and `MyAthleteDetailPage.tsx` is deleted in favor of `shared/StatCard`; the auto-select-growth effect (`hasSetInitialTab`) is deleted, so the athlete page now opens on "Info general" by default and only lands on "Crecimiento" via `?tab=growth`; the PHV colour maps in `TrainingReadiness.tsx` and `AthleteDetailPage.tsx` are deleted in favor of shared `StatusBadge` tone mapping (Pre = neutral, Circa = warning, Post = success). **Family mode**: a new `audience: "family" | "coach"` parameter on the PHV-explainer use case serves the same cached explanation in two registers from one prompt registry entry each (`phv_explanation_coach_v1.md` added alongside the existing family prompt), gated so only coach/admin can request `audience=coach` (403 for a parent, including on the cached `GET`); `FamilyStageCard`/`FamilyBandCards` and a `preset="family"` on the shared curve replace the parent view's reuse of coach-grade components, closing the FR-016 gaps (no Z-score, no percentile, no PHV/PWV marker, no numerals) and the latent bug that classified the parent view's **oldest** record instead of the latest (`MyAthleteDetailPage.tsx`, both the PHV-age lookup and the classification-card prop). **Infra bug found, not fixed here (pre-existing, outside this feature)**: the historical migration `backend/alembic/versions/e1f2a3b4c5d6_technique_gymkhana_library.py` imports `app.data.technique_catalog` at `upgrade()`-time, but that module was deleted by feature 038's cleanup (commit `718d249`) — any fresh database (new contributor, CI-from-scratch, disaster recovery, or this feature's own isolated e2e stack) fails `alembic upgrade head`; two sibling migrations (`f1a2b3c4d5e6_add_layout_json_to_technique_exercises.py`, `a7b8c9d0e1f2_strength_training_library.py`) share the same defect. This is why none of this feature's five Playwright specs (`growth.spec.ts`, `growth-parent.spec.ts`, plus the updated `history.spec.ts`/`anthropometry.spec.ts`/`target-size.spec.ts`) could be executed — written and statically valid, never run green; production and the developer's own compose stack are unaffected because their volumes already recorded the migration as applied. No new runtime dependency; backend + frontend, one migration, additive-only.
- Bitácora de etapa generated from Claude Code (skill `.claude/skills/bitacora-pdf/`, 2026-09-08): two new scripts, `backend/scripts/bitacora_snapshot.py` (reads prod MySQL through `AsyncSessionLocal` using only the `MYSQL_*` keys of `backend/.env.production`, reuses `build_newsletter_metrics` + `_build_forbidden_names` + `_previous_stage_texts` + `_resolve_family_insight`, adds per-session `individual_feedback`, Strava volume/interval matches, real `RaceEvent`/`RaceEventRoster`/`CalendarEvent` upcoming items with the hard-coded Copa Valle list as fallback, and `RaceResult.coach_note`/status/conditions; always ends in `rollback()` so the badge flush inside the builder is discarded) and `backend/scripts/bitacora_render.py` (no DB: applies `StageNarrativeGuardrails` + the `_accept_*` helpers of the v2 use case to a hand-written `narrative.json`, grounding numbers taken from the anonymized `brief.md`, then `build_stage_log` → `to_parent_dto` → `generate_stage_log_pdf`; exit 2 on any guardrail fallback; re-execs with `DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib` on macOS so WeasyPrint finds Homebrew pango/cairo). Output lives in `output/bitacora/<YYYY-MM>/athlete-<id>/` (new `.gitignore` entry — real minors' data). Nothing is persisted or emailed. Found while wiring it: prod is at Alembic `8b5ac1f24f61` while `main` head is `2a8baa967cc6` (feature 040 `growth_source`), i.e. features 039/040 are merged but not deployed (Render auto-deploy is off); the snapshot must run from a worktree of the deployed commit until then. Also confirmed the in-app newsletter never attaches the PDF (`attachments=[]` in `newsletter_dispatcher._send_v2_email`) and takes the next race from a literal list (`_get_upcoming_copa_valle_races`), not from `RaceEvent`. Privacy audit (`data-privacy-guard`) fixes applied the same day: calendar titles and `technical_focus` now go through `_redact_names` in `brief.md`, `--coach-note` is redacted before `build_stage_log` (same rule as the PATCH router), `roster.note` is no longer collected, the snapshot never prints raw tracebacks (DSN could leak), and `Settings.database_url`/`database_url_sync` now `quote_plus` user/password (prod password contains URL-reserved characters). Known gap: the product's own 3-page valve does not hold when the growth annex includes percentile curves — the athlete-2 August PDF renders 6 pages. Second gotcha, caught by the coach reviewing the first PDF: running the snapshot from the *deployed* commit (718d249) produces a pre-039 `metrics_snapshot` with no `cups` key, so the PDF template falls into its documented legacy path (`has_legacy_season_data`) and draws ONE mixed season line putting Copa Valle válidas, the Campeonato Departamental and the Campeonato Nacional on the same axis — precisely the comparison feature 039 exists to prevent. Fix: snapshot from a worktree of 84c8061 (feature 039 — has `comparison_groups.py`, predates the `growth_source` migration), which yields `cups[]` + `championships[]` and renders one chart per cup plus separate championship cards. `render_brief` had the same defect in text form (it flattened `progression_history`, labelling both championships as `V1` because their `sequence_number` is 1) and now emits one progression line per cup plus a per-championship line stating the field size and that it must not be compared with the copa. Follow-up asked by the coach: the season page showed `Gap al P1` and `Percentil` with no explanation for families, so `athlete_stage_log.html` now spells both out — the gap chart caption became "Gap: cuánto tardó de más que el mejor tiempo, en % de ese tiempo" (same single line, no extra height) and the championship footnote carries the long form with a worked example (1,5 % of a 40-minute race ≈ 36 s) plus "percentil 75 = llegó adelante de 75 de cada 100". Placement is load-bearing, not cosmetic: the heaviest-month budget is exhausted, so ANY extra line in the season section (a glossary box, or a longer chart caption) pushes that PDF to 4 pages and fails `test_heaviest_month_with_annex_still_within_three_pages`; the championship footnote is the only safe host because it renders solely when `championships[]` is non-empty, which that test does not exercise. 61 PDF tests green. Note these template tests need `DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib` locally or 13 of them fail on WeasyPrint's `libgobject-2.0-0` before asserting anything. Badge credibility fix, same day, prompted by the coach spotting a "Top 10" badge on a race with 7 starters: `badge_evaluator.py` gained `_is_credible_position(position, field_size)` (front half of the real field, `position * 2 <= field_size`, plus an absolute floor of 4 starters) and counts `field_size` as FINISHED-only results per `(event_id, category_id)`, the same convention `field_metrics.py` already uses — which is also why a championship card can read 5 while the table holds 6 rows, an intentional DNF/DNS exclusion, not a bug. `first_podium` now requires `position <= 3` instead of `<= 5`: the label always promised a podium while the code awarded down to 5th, and with this club's real fields of 5 to 7 riders a "top 5" distinguished nothing. Label stays `Primer podio` (the badge fires once per athlete, so dropping "Primer" would make a milestone look recurring). Net effect on the real 2026 season of athlete 2: only P3-of-6 and P2-of-6 earn anything; the reported P5-of-7 earns nothing, and `top10` never fires at these field sizes. Second bug fixed in the same pass: `_upsert_badge` stored `earned_at=datetime.now()`, so an August newsletter printed badges dated the generation day (8 September) on the "Ruta del mes" timeline; badges now carry the real session or race date, falling back to the period's last day, which also makes re-evaluation idempotent under the delete-and-reinsert pipeline. Already-awarded badges self-correct on the next evaluation, no migration. 401 tests green. The pre-existing `F401` on the `RaceSeries` import inside the `try/except ImportError` availability probe is untouched and intentional. Same-day UX pass over the rendered PDF (a `ux-researcher` agent read the two real August documents; fixes applied by a second agent plus this session): Colombian decimal comma is now a central concern — new `app/services/utils/numbers_es.py` exposes `format_number_es(value, decimals, sign, trim_zero)`, registered as the Jinja filter `num_es` next to `date_es` in `document_generator.py` and called from `stage_log_builder.py` / `newsletter_static_copy.py`, so "26.0 horas" reads "26 horas" and "+8.6 %" reads "+8,6 %"; free text inside `narrative.json` never passes through Jinja, so the skill's `references/narrativa.md` now instructs the writer to use commas directly (safe because `extract_numeric_tokens` normalizes `8,6` and `8.6` to the same grounding token, verified). The three percentile curves moved into the existing two-column `display:table` layout, which also surfaced a real bug: the macro was invoked without `width`/`height`, so the 500x320 defaults fought the 400x260 `viewBox` that `growth_chart_builder.py` actually emits and the axis ticks did not line up with the plotted data; passing `chart.width`/`chart.height` fixed the alignment and collapsed the heaviest month from 7 pages to 4. Growth-annex jargon is gone (`Offset` → `Desfase de maduración`, raw `PHV` column dropped as redundant with the worded `Maduración` state, percentile/z-score glossary added), `RPE promedio` became `Esfuerzo (0–10)`, and `Campeonatos` became `Campeonatos de la temporada` since the cards are season-wide inside a monthly document. `charts/gap_pct.svg.jinja` no longer drops null points silently — a missing gap renders as a hollow dotted marker labelled `s/d` with the line broken either side, so the gap chart can no longer show fewer points than its paired position chart without saying why (real case: válida 3 of athlete 6 has a position but no gap). Finally, `championship_card.html.jinja` suppresses the percentile below a field of 5: a 3rd-of-3 was printing `Percentil 0` in a family document, which reads as a verdict on the child rather than a statistic with no sample, the same small-field reasoning as the badge guard. 404 tests green. Follow-up on the coach's feedback that the percentile wording was still unclear: the championship-card footnote was not merely unclear, it was wrong — it read "a cuántos competidores de cada 100 de su categoría les llegó adelante", but `field_metrics.py:142` computes `100 * (1 - (position - 1) / (n - 1))`, i.e. the share of the OTHER starters finishing behind, normalised so first place is always 100 and last is always 0. Saying "de cada 100" is nonsense on a 5-rider grid. It now reads "de sus rivales de categoría, qué porcentaje terminó por detrás" with the anchors spelled out and a worked example matching the card printed right above it (P2 of 5 → 75, three of the other four). The growth-annex glossary was accurate but abstract, and it uses the same word for a different concept (reference population, not rivals), so it now leans on a concrete image — a line of 100 children of the same age and sex, how many fall below — plus an explicit "no es una nota ni un objetivo", which matters because that annex prints a P5 for height and a family should not read it as a grade. Both glosses live in footnotes that only render with their own section, so the 3-page budget is untouched; the heaviest month still renders 4 pages with the annex and the season section. 404 tests green. Calendar source fixed the same day after the coach asked where the newsletter got "Roldanillo, 12 September" from: `newsletter_builder._build_calendar_block` was reading a literal Python list, `_get_upcoming_copa_valle_races`, whose Roldanillo entry was five months off — the official Comisión Vallecaucana poster puts Roldanillo on 7-8 November (XCO on the Sunday) and Yumbo on 18 October, with no September date at all. The block now calls the new `_get_upcoming_races(db, after_date)`, which reads `race_events` ordered by date and only falls back to the literal list when the table holds no future race, so a club that has not loaded its calendar still gets a populated "Próximo tramo". The fallback list itself was corrected to the full official calendar. Root cause of the silent staleness: production held zero future races (last row was V Válida Palmira, 2 August), so the fallback was always winning. `scripts/seed_calendar_2026.py` loads what was missing — a new `Copa Let's GO Interdepartamental` series plus Alcalá 13 Sep, Yumbo 18 Oct and Roldanillo 8 Nov, each with its paired `competition` calendar event and both sides linked. It is idempotent on `(series_id, event_date)` and dry-runs by default, needing `--commit` to write. Note `race_series.level` has no `interdepartamental` member, so the Let's GO series is stored as `departmental`; changing that needs an enum migration. Adding the query shifted the positional mock sequence in `test_newsletter_builder.py::test_build_newsletter_metrics_has_both_blocks`, which asserts by call index and now needs its entry at position 9. Open data bug, not touched: `calendar_events` row 63 dates V Válida at 2026-09-02 while `race_events` row 26 has 2026-08-02, a month apart, which is why the athlete brief was announcing a club event on 2 September. 404 tests green. Both writes ran against production the same day, on the coach's explicit instruction: `seed_calendar_2026.py --commit` created series 9 (Copa Let's GO Interdepartamental) plus race events 30/31/32 (Alcalá 13 Sep, Yumbo 18 Oct, Roldanillo 8 Nov) with calendar events 83/84/85 linked both ways, and the new `scripts/fix_calendar_race_dates.py --commit` realigned the one mismatched row — «V Válida» sat at 2026-09-02 while its race was 2026-08-02. That second script generalises the fix rather than hardcoding the id: it joins `calendar_events` to `race_events`, treats the race date as authoritative and keeps the event's time of day, so it is idempotent and re-runnable (a second pass now reports nothing to correct). With real events loaded, the newsletter's "Próximo tramo" reads the database for the first time and announces Alcalá this Sunday instead of the phantom Roldanillo, which also meant rewriting the `next_segment_text` of both August narratives. 404 tests green. Corrected once more when the coach produced the organiser's poster: the Copa Let's GO is a five-round cup, not a single race, and Alcalá (13 Sep) is its Gran Final — the club simply skipped rounds I to IV. `scripts/fix_lets_go_series_2026.py` renumbers Alcalá from 1 to 5, renames it, and backfills Quindío 8 Feb, Sevilla 12 Apr, Viterbo 24 May and Anserma 9 Aug as `completed` race events with NO paired calendar event, since the club did not attend and they exist only as series context. Order of operations is load-bearing: `uq_race_events_series_sequence` is unique on (series, number), so the final has to vacate slot 1 before Quindío can take it — inserting first fails with a 1062 duplicate, which is how the constraint caught the first attempt. 404 tests green
- Multi-coach governance, migration and audit foundation (feature 041, 2026-09-10): single Alembic revision `45cd705c6b54_multi_coach_governance.py` (`down_revision="2a8baa967cc6"`, feature 040's `growth_source` column — still the sole `main` head at branch time). Adds `audit_log` (append-only, `occurred_at` as `DATETIME(fsp=6)` on MySQL — the one column in this table that must not inherit the `fsp=0` truncation already present on `race_result_revisions.changed_at`/`race_competitor_link_audit.created_at`; five composite indexes, no index on `occurred_at` alone), the `training_session_coaches` bridge table (composite PK, backfilled one row per existing session with `coach_user_id = created_by_user_id`), and additive nullable attribution columns (`updated_by_user_id`, `deleted_by_user_id`/`deleted_at`, `approved_by_user_id`/`approved_at`, `recorded_by_user_id`, `edit_version`, coach-note author, `decided_by_user_id`/`decided_at` on `agent_runs`, `committed_by_user_id`/`committed_at` on `race_imports`) across 14 existing tables via one new `ActorTimestampMixin`/`UpdatedByMixin` pair in `app/models/mixins.py`. All backfills are idempotent and dialect-aware (verified by review; not yet run against a live MySQL — see below). No new runtime dependency.
- Multi-coach governance, request-id and structured logging (feature 041, 2026-09-10): `backend/app/services/request_context.py` (new) holds a `ContextVar[str]` for the current `request_id` plus non-HTTP helpers (`system_context`, `webhook_context`, `cron_context`) for callers outside a request — the retention CLI, webhooks, cron-style scripts. A pure-ASGI `RequestIdMiddleware` (no new dependency — Starlette, same idiom as every other middleware in the app) is registered as the **outermost** middleware in `app/main.py` (last `add_middleware` call) and stamps `X-Request-Id` on every response; `CORSMiddleware` gained `X-Request-Id`/`ETag` in `expose_headers` and `If-Match` in `allow_headers` in the same change, since a concurrency or correlation contract that the browser client cannot see through CORS does not exist for it. Separately, `app/main.py` gained a `logging.config.dictConfig` block for the `app.*` logger namespace (`disable_existing_loggers: False`) with a `RequestIdLogFilter` injecting the current `request_id` (or `"-"` outside a request) into every `app.*` record — before this change, business logs from `app.services.*` were not reliably reaching uvicorn's own handlers in production, an existing gap this feature needed closed so an audited action and its log lines could be correlated by `request_id`. `record_audit` (`app/services/audit.py`) threads the same `request_id` into every row it writes, which is what makes a single request that touches several records (e.g. a session roster save) group as one operation in the club history (FR-002).
- Multi-coach governance, club-scope rule replaces the creator-lock on AI runs and race imports (feature 041, 2026-09-10): `_ensure_run_owner` deleted; seven call sites move to two new `permissions.py` helpers, `ensure_run_club_access`/`ensure_import_club_access` — any coach of the club can now read, decide, cancel and re-execute a run, or review/commit an import, that another coach of the same club started; a coach of another club is refused; an administrator always can (FR-028). Run→club resolution prefers `agent_runs.athlete_id`, falling back to `input_json["athlete_id"]` for the two insert sites that leave the column `NULL`. Import→club resolution has no `club_id` to read from (`race_imports`/`race_series`/`race_events` are third-party competition data, not club-owned) so it resolves through the importer's own `club_members` row — see the H7 finding below for the gap this creates. `agent_runs` gained `decided_by_user_id`/`decided_at`; `race_imports` gained `committed_by_user_id`/`committed_at`; both response schemas now surface who launched and who decided/committed. AI spend is now shown per coach (`budget_guard.py::spend_by_user_last_30d`, grouped by `generated_by_user_id`, "Sin atribuir" bucket for pre-existing rows) while the budget itself stays one club-wide limit — visibility changed, not the limit. **A security review of this exact change (T080, corrida 2 of the night run) found two real cross-club leaks reaching a minor's data, both fixed and committed the same day (`8ca2870`)**: (H1) `POST /api/race-analysis/runs` and its group-launch sibling did not check the target athlete's club at all — a coach of another club could launch an AI analysis on a minor who was not theirs, whose name then reached `forbidden_names` and the AI provider, got a persisted insight, and an audit row stamped with a club and actor that do not belong to her; fixed in `resolve_group_members`, with the club check ordered **before** the archived-athlete check so a 404-vs-403 timing difference cannot tell an outside coach whether an id exists. (H2) `GET /race-events/{id}/runs` handed out another club's minor's full name, athlete id, and a valid `run_id` (the only practical way to obtain one, since they are `uuid4`) to any authenticated coach — the paired detail endpoint was already correctly closed (403), so this was a listing/detail asymmetry in the worst direction. Both root-caused to the same gap: the contract had marked these two launch/list surfaces "unchanged, already unfiltered by actor" — correct for *actor*, not for *club*, and "create" a new run never appeared in the operate-on-an-existing-record matrix that drove the rest of this change. Six further findings from the same review (H3–H7) touch only adult staff data (names, spend figures) or endpoint-shape inconsistencies, not a minor's data; all six were closed in the same night run's final pass (corrida 3, commit `a943642`, together with T030's last route and a T091 privacy-audit fix): **H3** `GET /admin/ai-usage`, reachable by coaches, returned every club's staff spend, not just the caller's own (`race_analysis.py` RBAC + `budget_guard.py::_QUERY_SPEND_BY_USER`) — fixed with a new `_coach_visible_staff_ids` helper and a `visible_user_ids` parameter on `spend_by_user_last_30d`: a coach now sees identity and individual spend only for staff of their own club(s), everyone else's cost is folded into one unnamed "Otros clubes" bucket, and the FR-029 reconciliation invariant (sum of returned rows equals the club-wide total) is preserved by summing rather than dropping the folded cost; admin behaviour is unchanged (`visible_user_ids=None`). **H4** `GET /imports/` returned every club's import list to any coach — fixed by filtering in SQL (so `total`/pagination stay correct, not just the rendered page) to imports uploaded by a coach-or-admin member of the requester's own club(s), with an authorship fallback that never widens access. **H5** the stale `user#{id}` fallback in the imports listing (contradicting FR-013) is replaced with the same "Usuario no disponible" text used elsewhere in the feature. **H6** the RBAC docstring and comment on `/admin/ai-usage` now state its real coach+admin scope instead of implying admin-only; the route path is unchanged for frontend contract stability, and `_admin_only` stays defined because test files outside this feature's scope still import it for dependency overrides. **H7** `import_club_ids` (`permissions.py`) now resolves through `(coach, admin)` memberships instead of `coach`-only, so an import uploaded by the club's administrator is reachable by its coaches again — the sibling `run_club_ids` deliberately keeps the narrower `coach`-only resolution, since the two answer different questions (a parent who is also a coach of another club must never leak a club through the run path). New tests: `backend/tests/test_race_imports_club_scope.py`, `backend/tests/test_spend_by_user.py`; `backend/tests/routers/test_race_event_runs.py` fixtures updated to declare the requesting coach's club, made mandatory by the corrida-2 scope fix. Same pass also closed **T030** (the last `pending instrumentation` route, the import dry-run, resolved to a genuine §4.14 exemption — see the separate technical-notes entry below) and a **T091 privacy-audit finding**: `TrainingSessionReadParent` was returning `created_by_user_id`, a raw unresolved staff id, to families — dropped from both the Pydantic schema and the response-model exclusion in `training_sessions.py`, extending the existing "no coach identity reaches a parent" rule from names to raw ids. Full detail: `docs/19-multi-coach-governance/qa.md` §5, `specs/041-multi-coach-governance/checklists/privacy-audit.md`, `specs/041-multi-coach-governance/checklists/integration-review.md` ("Corrida nocturna 2" section, §6).
- Multi-coach governance, pre-existing migration bug that blocks a fresh database (feature 041, 2026-09-10): the same defect feature 040 had already documented and worked around on its own branch (`docs/technical-notes.md`'s 2026-09-05 entry) resurfaced on `main` for this feature, because that fix had not reached `main` — three historical migrations (`e1f2a3b4c5d6_technique_gymkhana_library.py`, `f1a2b3c4d5e6_add_layout_json_to_technique_exercises.py`, `a7b8c9d0e1f2_strength_training_library.py`) import `app.data.technique_catalog`/`app.data.strength_catalog` at `upgrade()`-time, both deleted by feature 038's cleanup, so `alembic upgrade head` from an empty database fails on any of the three before this feature's own migration is ever reached. Fixed the same way as before — each import wrapped in `try/except ImportError`, logging and skipping the now-impossible seed step rather than failing the chain, since the seeded rows are dropped by a later migration (`d0e1f2a3b4c5`) regardless — and committed on this branch (`c596622`). **Unverified in this environment**: no Docker and no reachable MySQL were available in any of the three night runs that built this feature, so `alembic upgrade head` from empty was never actually executed here — this is a code-reviewed fix to a known-shaped problem, not a confirmed-working one. Whoever next has a live database should run the chain from empty as the first check; see `docs/19-multi-coach-governance/qa.md` §2 and §4 and `runbook.md` §5.8 for the complete list of what is written-but-unverified for this reason.
- Multi-coach governance, `race_events.py` coach-only follow-up, logged not fixed (feature 041, 2026-09-10): two endpoints, `backend/app/routers/race_events.py:606-609,700-703`, require the `coach` role and refuse an administrator with `403` — inconsistent with every other mutation in that router, which allows both roles. Pre-existing, unrelated to attribution or to this feature's own RBAC changes; identified during planning (`specs/041-multi-coach-governance/research.md`, `plan.md` Complexity Tracking) and deliberately left as-is rather than changed silently as a drive-by fix, since it is a one-line decision for the product owner (should an admin be able to do this too?) rather than an audit-correctness question. Raise it with the owner before the next `race_events.py` change.
- Multi-coach governance, `mysql` lane executed for the first time (feature 041, 2026-09-10, local): the isolated e2e stack booted on an empty volume (the T001 migration fix is now confirmed live) and `pytest -m mysql` ran against a throwaway MySQL 8.4 `_test` database — 17 passed after four harness defects were fixed: (1) `tests/test_audit_mysql.py` walked Alembic in the same schema on which the session-scoped `mysql_session` keeps a transaction open, so its `DROP TABLE` waited forever on metadata locks — it now migrates a sibling `<name>_migrations_test` database it creates and drops; (2) `backend/alembic/env.py` unconditionally overwrote `sqlalchemy.url` with the app settings — it now honours `Config.attributes["sqlalchemy.url"]` (CLI and `entrypoint.sh` unchanged); (3) the pre-041 raw-SQL seed missed NOT NULL `created_at` columns and T20's audit rows violated the real `club_id` FK; (4) newsletter concurrency case 8 was an unconditional `pytest.skip` — it is now a real two-session InnoDB row-lock test. The `client`-fixture tests also ran against a migrated + seeded throwaway database via `MYSQL_*` overrides: all green except three tests that were already stale on `main` (`test_past_date_returns_422`, `test_set_audiences_borra_y_reinserta`, `test_event_data_competition_valid`), previously hidden inside the environmental baseline.
- Multi-coach governance, commit-after-response race on staff writes (feature 041, 2026-09-10): FastAPI ≥0.118 (0.135.3 here) sends the response *before* closing request-scoped `yield` dependencies, so `get_db`'s implicit commit lands after the client already has its 201; the immediate TanStack refetch on `/admin/usuarios` read the list without the new account (E2E-STAFF-004, deterministic). `POST /api/users` and `PATCH /api/users/{id}` now `await db.commit()` before returning, the pattern 20 routes already follow. **The app-wide exposure is not fixed**: every route that relies on the implicit commit has the same read-after-write window — owner decision between explicit commits per route and `Depends(get_db, scope="function")` (the latter closes the session before background tasks that still use it).
- Multi-coach governance, live-review fixes (feature 041, 2026-09-10): (a) `POST /api/training-sessions` never validated `convocados_athlete_ids` (pre-existing on `main`); with archiving it let an archived or other-club athlete be convoked — it now returns the same 400 as the convocatoria update route; (b) the club-history 403 for parent/athlete uses the generic copy of `contracts/audit-log-api.md` §4.4; (c) every audit sentence with `{fecha}` degraded to the generic §7.4 form whenever the write did not change the date — writers now persist the entity date in `meta_json.event_date` (session update/execute/cancel/route-file, calendar event create/update/cancel/delete, anthropometric record create, anthropometry backfill jobs); (d) tests aligned with 041 contracts (staff creation without password and with club, cancellation with `reason_code`), the H1/H2-stale race-analysis doubles given club memberships, and `datetime.test.ts` made independent of the machine timezone; (e) e2e: `ConfirmDialog`-based dialogs are `alertdialog`, the synthetic athlete needs `club_id`, `SessionCoachesField` needs the injected `user`, `dashboard-coach.spec.ts` mocks `/api/users` (for `ActorChip`) and `/api/audit/reason-codes`; `App.tsx` opens the four `/athletes*` routes to admin (archive is `[admin, coach]` at the API).
- Pre-existing issues surfaced during the 041 live review, logged not fixed (2026-09-10): training-session e-mail subjects render English month names (`strftime("%-d de %B de %Y")` in `app/services/training/sessions.py` under the container's C locale — same on `main`, `format_date_es` already exists); `LLMSchemaError` from the monthly-report word-floor guardrail surfaces as a 500 (no handler on `main` either; with `AI_ENABLED=false` the fake provider can never pass it, so monthly reports cannot be generated on the isolated stack); contract conflict on `DELETE /api/users/{staff}` (`contracts/staff-admin.md` §5 keeps the unconditional 403, `contracts/athlete-archive.md` §8 and the quickstart expect 409); `frontend/e2e/invitations.spec.ts` is written against ids of the real `me` database.
- Multi-coach governance, closing-pass test state, said plainly (feature 041, 2026-09-10): the final offline `pytest` run of the third night run measured 233 failed / 4432 passed / 40 skipped / 13 xfailed / 6 xpassed / 9 errors against a `main` baseline of 224 red identifiers, all pre-existing and environmental (the `client` fixture needs a real database, `checklists/baseline-main-failures.txt`). The delta is **16 real regressions, one root cause**: the H1/H2 club-access fix (`8ca2870`) made a club-membership check mandatory on the race-analysis run-listing path, and several test doubles in `tests/routers/test_race_analysis*.py` and `tests/routers/test_race_event_runs.py` build a coach with no club membership, so they now hit `403` where they used to hit `200` — the production code is correct, the fixtures are stale. 6 of 9 cases in `test_race_event_runs.py` were fixed in the same pass by giving the fixture a club (the same `user_id * 1000 + 1` seed `_seed_base` already uses); 3 remain in `TestListEventRuns`, traced to that class's own `_seed_agent_run` helper, not finished for lack of time. Do not revert the club-access check to turn these green. Separately, `backend/tests/test_race_imports_club_scope.py` (new, H4's tests) now shares a basename with the pre-existing `backend/tests/routers/test_race_imports_club_scope.py`, whose `test_listado_identico_para_los_tres_coaches` (~line 462) asserts the **old**, unfiltered `GET /imports/` behaviour H4 deliberately changed — needs updating or retiring, not left green by accident. `frontend/src/types/trainingSession.types.ts` still declares `created_by_user_id` as required on the parent-facing type even though the backend stopped sending it to families (the T091 privacy fix above) — a type that lies, not a runtime break, since nothing validates a response against it. T078 (US6 backend tests) closed 2 of 3 files (`test_spend_by_user.py` 7/7, `test_race_imports_club_scope.py` 7/7 green); `test_race_analysis_club_scope.py` was not written. `ruff check`: 366 findings on the branch vs 345 on `main` — 19 of the 21-item delta are cosmetic `E402` in new test files, 2 were real `F821` (undefined name) in `app/dependencies.py`, fixed in this pass by moving two forward-reference-only imports under `TYPE_CHECKING`. Full detail and the decision log (D1–D9) covering every judgment call across all three night runs: `specs/041-multi-coach-governance/checklists/integration-review.md` ("RESUMEN FINAL DE LA NOCHE — corrida 3" section).
- Race AI local provider `claude-cli` (2026-09-10): fourth `RACE_AI_PROVIDER` value backed by the PyPI package `langchain-claude-cli` (`ChatClaudeCli`, wraps `claude-agent-sdk`, reuses the developer's Claude Code OAuth login from the macOS Keychain — no API key). **Local development only**: Anthropic's consumer terms forbid using a personal subscription to serve end users, so the package is deliberately NOT in `requirements.txt` (commented pointer only; Render never installs it) and the builder raises a clear `ImportError` when missing. Builder `_llm.py::_build_claude_cli_llm` ignores `temperature` (claude 4.6+ rejects sampling params), `max_output_tokens` (the package implements it as client-side truncation at `max_tokens*4` chars, which would cut the analyst's structured JSON), `api_key`/`base_url`; `timeout` is `max(AI_TIMEOUT_SECONDS, RACE_AI_V3_TIMEOUT_SECONDS)` because in this package it aborts the whole CLI run, subprocess startup included. `pricing.py` rates it at 0 USD (same precedent as Ollama), so the budget guard never trips for it. `.env` gotcha: `RACE_AI_ANALYST_MODEL`/`RACE_AI_CRITIC_MODEL` default to Gemini ids and must be set to Claude models (`claude-sonnet-5` / `claude-haiku-4-5-20251001`). Verified live: `call_llm`, `with_structured_output(..., include_raw=True)` and real `usage_metadata` work; every call carries ~22k cached tokens of CLI system prompt and ~7 s latency on a trivial prompt, so it burns subscription quota faster than the prompt size suggests. Tests: `test_llm_helpers.py` (+5), `pytest tests/services/race` green. **Provider unification across both AI stacks (same day)**: the two stacks (`app/services/ai/` for newsletter/session-assistant/monthly-report/PHV-anthropometry-explainers, and `app/services/race/agents/` for the race pipeline) had independent provider variables that had to be kept in sync by hand — the exact trap that caused the specs/036 T051 incident (see `.github/workflows/race-eval.yml` comment) when they drifted. Fixed the ergonomics without merging the two abstractions (one is a hand-rolled `LLMProvider` Protocol with `complete`/`complete_json`, the other is LangChain-based for LangGraph tool-calling/structured-output — merging them would mean either losing `with_structured_output`/`bind_tools` on the race side or rewriting four provider adapters on the `ai/` side, out of scope and risky for a golden-eval-gated pipeline): `Settings.race_ai_provider` now defaults to `""` instead of a hardcoded `"google"`, and every reader (`_llm.py::build_chat_llm`/`resolve_configured_model`/`call_llm`, `analyst.py`'s `_generate_v3`) resolves `race_ai_provider or ai_provider` instead of falling back straight to `"anthropic"`. `Settings.validate_race_ai_provider` accepts empty string as "inherit". CI is unaffected (`RACE_AI_PROVIDER: google` set explicitly in the workflow); Render is very likely unaffected too since both current defaults already agree (`google`). Added `claude-cli` as a fifth provider to the `app/services/ai/` factory too (`ClaudeCliProvider`, `app/services/ai/providers/claude_cli_provider.py`) — wraps `langchain_claude_cli.ChatClaudeCli` and translates to/from `LLMRequest`/`LLMResponse`, `complete_json` uses the same prompt-injection strategy as `AnthropicProvider`/`OpenAIProvider` (not native structured output) for consistency with the rest of that stack. `docker-compose.yml`'s `RACE_AI_PROVIDER` default changed from `anthropic` to empty so the container gets the same inheritance. Verified live end-to-end on both factories (`app/services/race/agents/_llm.py::build_chat_llm` and `app/services/ai/factory.py::create_llm_provider`) with `AI_PROVIDER=claude-cli` as the only provider variable set. `CLAUDE.md`'s two-stacks section updated to describe the inheritance instead of claiming an independent `"google"` default. End-to-end check on the local DB (uvicorn on :8001 with the provider set via env vars, one válida): run completed in ~137 s, `athlete_ai_insights` row persisted with `model=claude-sonnet-5`, `prompt_version=race_analyst_v3`, `is_fallback=False` (structured-output path), 7 sections / 428 words; `agent_runs.status=completed`. **Docker bridge (same day)**: the container can run the provider too. `backend/Dockerfile` takes build arg `INSTALL_CLAUDE_CLI` (default `false`; Render builds the same Dockerfile without it, so the production image is unchanged) and pip-installs `langchain-claude-cli==1.2.1`, whose `claude-agent-sdk` manylinux wheel bundles the CLI — no Node in the image. The macOS Keychain login is not reachable from Linux, so `docker-compose.yml` forwards `CLAUDE_CODE_OAUTH_TOKEN` (minted by each developer with `claude setup-token`, 1-year, inference-scoped, kept only in the gitignored `.env`) plus `RACE_AI_ANALYST_MODEL`/`RACE_AI_CRITIC_MODEL`, which compose never forwarded before. Their compose defaults replicate `config.py` (`gemini-3.8-flash` / `gemini-3.1-flash-lite`) because an empty env var overrides a pydantic-settings default (`env_ignore_empty` is not set) — the same trap `.env.example` briefly had (fixed by commenting those keys out). Consequence: switching Docker back to Gemini means unsetting the role-model vars as well, not only `RACE_AI_PROVIDER`. **Dead "Sesiones de fuerza" field removed from the analyst prompt (2026-09-10)**: the strength/technique exercise catalog was retired by migration `d0e1f2a3b4c5_remove_technique_strength_catalog.py`, but `analyst.py::_training_window_block` kept rendering `- Sesiones de fuerza: {tw.get('strength_sessions')}` — a key `athlete_context.py::load_training_window` never populated, so it always printed `—` and let the LLM invent "no registró sesiones de fuerza" from a placeholder. Fixed by (1) dropping that half of the line, keeping `- Sesiones con intervalos: N` (the intervals catalog is still live) as its own line; (2) adding `training_window.session_descriptions` — the free-text `TrainingSession.description` field ("Descripción" in the session wizard, where the coach writes what was actually done, including strength work) sorted most-recent-first, capped at 3 entries / 200 chars each, same bounds as `coach_feedback`; rendered as its own "Descripción de sesiones recientes" block. `anonymize.py`'s existing `training_window.coach_feedback` scrub (club-wide forbidden-names superset, since session text can name teammates) was extended to cover `session_descriptions` too — this is the only source of PII risk this change introduces, and it reuses the exact same `_scrub_note` path.
- Local-only Langfuse tracing for race AI (2026-09-10): new `app/services/race/observability.py` wraps LLM calls (`race-analysis` — graph start and HITL resume share one trace via a deterministic id seeded from the external run id; `race-chat`; `race-eval-judge`, both v1 and v2) with `langfuse.langchain.CallbackHandler` (SDK `langfuse>=4.6,<5`, OpenTelemetry-based — not the SDK 3.x `@observe` decorator the original F8B plan assumed). Gated by `LANGFUSE_ENABLED` (default `false`); `Settings.forbid_langfuse_in_prod` raises at startup if it's `true` under `APP_ENV=production`, so this never runs on Render. **Privacy fix, post-write (same day)**: the original design had an opt-in content-passthrough setting that let pseudonymized `race-analysis` analyst/critic generation content through — removed after privacy audit, because even a pseudonymized prompt still carries quasi-identifiers of a minor (event date, category, race position, times, age, maturity/PHV data), and the analyst's recalled memory (rehydrated summaries) can contain real names. There is no content-capture toggle at all: inputs, outputs and metadata of every observation are always `"[redacted]"`, on every trace, no exceptions. Langfuse is used only for token counts (`usageDetails`), model, latency, and trace/session structure/tags. A `should_export_span` filter also drops any third-party OpenTelemetry span (e.g. MCP/`gen_ai` spans the local `claude-cli` provider emits on its own) so only spans this integration explicitly created reach the collector. Verified live 2026-09-10: a traced call showed `usageDetails` populated with `input`/`output` as `[redacted]`; the Langfuse v2 observations API needs `fields=core,basic,usage,io,model` to return usage/model fields at all. `agent_runs.langfuse_trace_id` is populated by `routers/race_analysis.py::_finalize_run` only when tracing is enabled. Traces persist in the local Docker volumes with no retention policy configured; purge with `docker compose -f docker-compose.langfuse.yml down -v`. Infra is `docker-compose.langfuse.yml`, a standalone Langfuse **v4** stack (Postgres 16, ClickHouse 26.4, Redis 7.2, MinIO, worker+web, 6 services — not the 2-service v3 sketch from the original F8B plan), headless-bootstrapped via `LANGFUSE_INIT_*` vars so the UI never needs to be opened to generate API keys; two v4-specific gotchas documented in the compose file's own comments: `langfuse-web` must not `depends_on` the worker (only web runs the Prisma migrations on first boot; waiting on the worker would deadlock, since the worker never turns healthy until that schema exists), and every healthcheck targets the service name (`http://langfuse-web:3000/...`) rather than `localhost`, because the Next.js server binds to the container's interface IP, not loopback. Docker backend reaches it via `LANGFUSE_BASE_URL_DOCKER` → `http://host.docker.internal:3001` (`docker-compose.yml`); host `uvicorn` uses the `http://localhost:3001` default. Cost figures shown in the Langfuse UI are approximate — it infers USD from its own model price list and doesn't recognize project-specific ids (`gemini-3.8-flash`, `claude-cli` at $0 local cost); `agents/pricing.py` + `athlete_ai_insights.metrics_snapshot_json` + the 30-day budget guard remain the source of truth. New runbook section: `docs/10-race-results/runbook-ops.md` §8 ("Local LLM tracing"). Obsolete "F8B" material describing a VPS/Hetzner production deployment, `app/observability/langfuse.py` + `FakeLangfuse`, and "Langfuse UI budget alerts" (never wired — the budget-overrun path stays log-only, per the documented TODO in `budget_guard.py`) removed from `docs/10-race-results/v2-agentic-design.md` and `v2-implementation-workflow.md`, with the original dated decision rows (A2, A8, 2026-05-20) kept and annotated "Superseded 2026-09-10" rather than deleted.
