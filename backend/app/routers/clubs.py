from fastapi import APIRouter

router = APIRouter()


# POST /api/clubs — Paso 4
# GET /api/clubs — Paso 4
# PATCH /api/clubs/{id} — Paso 4
# GET /api/clubs/{id} — Paso 4
# POST /api/clubs/{club_id}/members — Paso 4
