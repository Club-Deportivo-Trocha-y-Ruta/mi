export enum Sex {
  M = "M",
  F = "F",
}

export enum MaturationStatus {
  PrePHV = "Pre-PHV",
  CircaPHV = "Circa-PHV",
  PostPHV = "Post-PHV",
}

export enum UserRole {
  admin = "admin",
  coach = "coach",
  parent = "parent",
  athlete = "athlete",
}
