# Implementation Plans

Generated by the `improve` skill on 2026-06-11, against commit `9871c99`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Selection note: the advisor's findings table also recommended a "CI baseline +
dependency consolidation" plan (no PR CI beyond the race golden eval; ~23 prod
deps missing from `pyproject.toml` vs `requirements.txt`) as the top-leverage
item. The operator deselected it in this cycle — recorded here so the gap
isn't forgotten and isn't re-audited as a discovery.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Prod config hardening (JWT default hard-fail, CORS `*` warning, parser error sanitization) | P1 | S | — | DONE |
| 002 | Race-import commit row locking (double-submit IntegrityError) | P1 | S–M | — | TODO |
| 003 | Password-reset atomic token consume | P2 | S | — | TODO |
| 004 | Standings characterization tests + mutation on standings.py | P2 | M | — | TODO |
| 005 | Opt-in MySQL dialect test lane for raw-SQL race modules | P3 | M–L | — | TODO |
| 006 | INVESTIGATION: RaceImport/RaceEvent responsibility map; verify specs 007–009 consolidation | P3 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies; 001–004 are independent and parallelizable.
- 005 *complements* 002 and 004: the `FOR UPDATE` lock (002) and the
  `CAST(bool AS Integer)` podium aggregation (004) only exercise their real
  semantics on MySQL — plan 005 includes cross-check tests for both.
- 006 is read-only and can run anytime; any consolidation plan it spawns MUST
  land after 004 (standings characterization) and equivalent ingestor coverage.

## Findings considered and rejected

(So nobody re-audits these.)

- **"No CLAUDE.md"** — false positive; `/CLAUDE.md` exists and is substantial.
- **Email-change rate limiting unenforced** — false positive; enforced in
  `backend/app/services/profile.py:149-158`.
- **`auth-coach.json` committed credential** — benign: an empty Playwright
  storageState (`{"cookies": [], "origins": []}`).
- **Parents see z-scores/percentiles (re-identification)** — by design:
  parents view their *own* child's growth data; the AI-prompt exclusion of
  z-scores addresses a different threat model (third-party LLM).
- **Age-category timezone bug** — categories derive from birth *year* only
  (`backend/app/services/category.py:22`), immune to time-of-day; the
  `age_decimal` drift is ≤1 day (~0.003 yr). Not worth doing.
- **Pagination on clubs/events lists** — single-club product, ~8 events per
  season; unbounded queries are fine at this scale. Revisit if multi-club.
- **`qs` npm advisory** — dev-only transitive (via `typed-rest-client`);
  one-line `npm audit fix` chore, not plan-worthy.
- **SFTP move-failure swallowing in commit** — graceful degradation; the DB
  keeps the still-valid `pending/` path on failure, so no state divergence.
- **Chat `_SessionStore` uses `time.monotonic()`** — monotonic is the correct
  clock for TTL on a server; the sleep/skew concern doesn't apply to Render.
- **Speculative "future developer might log prompts / persist a new query
  key" guards** — current code is correct; guarding hypothetical future
  mistakes isn't a finding.
- **App.tsx eager/lazy import inconsistency (47 eager vs 8 lazy)** — real but
  low-value; deferred, not planned this cycle.
- **FTPS certificate verification disabled (minors' media, Hostinger)** —
  real and HIGH-sensitivity but the fix is a storage migration (Cloudflare
  R2) the operator deselected this cycle; the code comment in
  `backend/app/services/training/storage_sftp.py:52-63` already documents the
  accepted risk and the R2 exit path.
- **CLAUDE.md says spec 012 e2e is "pending local run"** — stale doc line;
  commit `bd9945b` ran and passed the 3 cold-start e2e tests.
