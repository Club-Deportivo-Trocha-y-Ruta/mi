# Checklist — Phase 2 Foundational (feature 040, growth module redesign)

**Wave**: Phase 2 — correctness first (no visual change) + blocking schema · **Date**: 2026-09-04 · **Branch**: `feat/040-growth-module-redesign`
**Gate**: `engineering-lead` (wave review) · **Workers**: `react-ui-engineer` (T005–T009, T014), `qa-engineer` (T004, T010, T011, T015), `database-architect` (T012), `fastapi-architect` (T013)

## Result

| Task | Owner | Status | Evidence (command → result) |
|---|---|---|---|
| T004 — regression test: parent view classifies the **latest** record | qa-engineer | **Done** | `frontend/src/routes/parents/__tests__/MyAthleteDetailPage.growth.test.tsx` created. Deliberately does **not** mock `useAnthropometry` — it drives the real hook/axios path against an inline MSW handler for `GET */api/athletes/:athleteId/anthropometry` returning 3 synthetic records newest-first. Worker confirmed **FAIL 2/2 before** the T005 fix (oldest record's band and `age_at_phv` were shown), **PASS 2/2 after**. Gate re-ran: `npx vitest run src/routes/parents` → **PASS 114/114**. Fixtures synthetic (`first_name: "Atleta"`, `last_name: "Ficticio"`, `birth_date: "2013-06-15"`) — no minor data. |
| T005 — `MyAthleteDetailPage.tsx` uses `records[0]` | react-ui-engineer | **Done** | `git diff frontend/src/routes/parents/MyAthleteDetailPage.tsx` → both call sites changed from `records[records.length - 1]` to `records[0]`: `phvAgeMonths` (line ~195) and the `NutritionalClassification record=` prop (line ~405). Ordering contract documented in an inline comment. Defect **G-03 closed**. |
| T006 — `bands.ts` → `NutritionalStatus` vocabulary, indicator-aware `classifyBand` | react-ui-engineer | **Done** | `BAND_VOCABULARY` keyed by the 9 `NutritionalStatus` values with `coachLabel`/`familyLabel`/`narrative`/`tone`/`referral` — gate diffed all 9 rows against `contracts/band-vocabulary.md`: **exact match** (incl. `talla_alta` = `neutral`, `sobrepeso` = `warning`; `referral: true` only for `retraso_talla`, `delgadez_severa`, `obesidad`, per `data-model.md` §76). `classifyBand(indicator, z)` cut-offs diffed line-by-line against `backend/app/services/growth.py::classify_nutritional_status_height` (L135–141) and `::classify_nutritional_status_bmi` (L159–166): **parity confirmed**, including the D2 fix (height/weight `−1 ≤ z ≤ 2` → `talla_adecuada`; `talla_alta` only `z > 2`). `bands.test.ts` has **10 z values × 3 indicators** via `it.each`, with the boundary cases (−2, −1, 1, 2, 2.01) explicit. `getBandSpec` retained as a compatibility layer for the not-yet-migrated callers. Defect **G-02 closed**. |
| T007 — `useGrowthMetrics` returns the new band keys | react-ui-engineer | **Done** | `git diff frontend/src/hooks/athletes/useGrowthMetrics.ts` → local 5-band `classifyBand` deleted; hook now calls the indicator-aware one and `GrowthMetrics.band` is typed `NutritionalStatus`. `GrowthBand` re-exported for the legacy `PercentileCurves.tsx` importer. Test file moved to `__tests__/` (folder convention); consumer tests updated. `npx vitest run src/hooks/athletes src/components/athletes` → **green** (see gate table). |
| T008 — remove the numeric max-HR estimate from `TrainingReadiness.tsx` | react-ui-engineer | **Done** | `grep -n "lpm\|197" frontend/src/components/athletes/TrainingReadiness.tsx` → **no matches**. Both forbidden branches (Circa-PHV and 10–12) collapsed into one rule whose detail reads exactly `"Sin test de FC máxima — no se estima ni se usa para zonas"` (decision D4). 2 new assertions that no `/lpm/i` text renders. Remaining `lpm` strings elsewhere (`ActivityCard.tsx`, `ActivityEvidenceStrip.tsx`) are **recorded Strava HR data, not estimates** — correctly left untouched. Defect **G-04 closed**. |
| T009 — extract `STATUS_META` into `lib/measurementStatus.ts` | react-ui-engineer | **Done** | New `frontend/src/lib/measurementStatus.ts` exports `STATUS_META`, `MeasurementStatusMeta`, `getMeasurementStatusMeta` and re-exports the `MeasurementStatus` type. `git diff frontend/src/components/dashboard/MeasurementAlerts.tsx` → table removed, single import added, **no behaviour change**; its 20 existing tests stay green. `lib/measurementStatus.test.ts` added. The unrelated `STATUS_META` in `components/intervals/PlanVsActualTable.tsx` (different domain) correctly left alone. Ready for T039. |
| T010 — decouple `e2e/history.spec.ts` from auto-select | qa-engineer | **Done** (Playwright execution deferred — see below) | `git diff frontend/e2e/history.spec.ts` → E2E-008 now performs an explicit `page.getByRole('button', { name: /crecimiento/i }).click()` before asserting on `growth-charts`; E2E-007's stale "auto-selecciona" comment reworded (it already clicked `Antropometría` explicitly, so it had no functional dependency). `npm run typecheck` → **PASS**. The suite itself was not executed: `frontend/e2e/*` requires `docker compose up` per the file header. |
| T011 — characterization tests for `PercentileCurves` | qa-engineer | **Done** | `frontend/src/components/athletes/PercentileCurves.characterization.test.tsx` — **17 tests / 5 describe blocks**, all passing. Pins: legend toggle (`aria-pressed` + `hide` prop flip, `band_*` keys excluded), bio-age toggle group (coach/admin-only, hidden for `bmi_for_age`, relabels the X tick from `15.0 a` to `+2.0 a`), maturation marker by indicator+sex (PHV / PHV·PWV / PWV / absent), sr-only table rows = athlete records with the date obfuscated to month-year and Z/percentile cross-checked at runtime against the real `lib/growth/lms.ts`, and the PNG export file name (`/^crecimiento-height_for_age-\d+\.png$/`, no identifying text). Follows the repo's recharts-mocking convention; **no source file modified**. Safety net for the T052 deletion is in place. |
| T012 — Alembic migration `growth_source` | database-architect | **Done** | `backend/alembic/versions/2a8baa967cc6_growth_source_on_anthropometry.py`. `cd backend && .venv/bin/alembic heads` → **`2a8baa967cc6 (head)` — single head**; `down_revision = '8b5ac1f24f61'` matches the prior head. Additive **nullable** `Enum('WHO','CDC', name='growthsource')`, no default, no backfill; downgrade drops the column. Worker verified `upgrade head → downgrade -1 → upgrade head` for real against the docker dev MySQL 8.4 (`SHOW COLUMNS` before/after), with `MYSQL_HOST=127.0.0.1` passed on the command line — `.env` never read or printed. Docstring records the risk (instant/in-place DDL on MySQL 8.0+) and the rollback plan. |
| T013 — model + `AnthropometryOut` field | fastapi-architect | **Done** | `git diff backend/app/models/anthropometry.py` → `growth_source: Mapped[GrowthSource | None]` with `Enum(..., values_callable=...)` per repo convention, reusing the `GrowthSource` enum that already backs `growth_reference_lms.source`. `git diff backend/app/schemas/anthropometry.py` → `growth_source: str | None = None`. New `test_growth_source_field_present_and_null_for_legacy_rows` asserts the key is present and `null` on the POST response, the GET list response and at ORM level. `create_anthropometry` intentionally untouched (that is T022). `pytest tests/routers/test_anthropometry_bmi.py tests/test_growth_service.py -q` → **84 passed**. |
| T014 — frontend type + Zod schema | react-ui-engineer | **Done** | `frontend/src/types/anthropometry.types.ts` → new `GrowthSource = "WHO" \| "CDC" \| null` and `growth_source?: GrowthSource` on `AnthropometricRecord`. **Judgment call accepted by the gate**: no anthropometry Zod schema existed under `frontend/src/schemas/` (tasks.md assumed one; the only Zod touching these fields is the inline create-form schema in `AnthropometryForm.tsx`, which is not a response schema). The worker created `frontend/src/schemas/anthropometry.schema.ts` mirroring `AnthropometricRecord` 1:1, following the `schemas/stageLog.ts` pattern. Correct call — T036 will add `growth.schemas.ts` next to it. |
| T015 — full offline lane + frontend gate | qa-engineer | **Done** (with pre-existing environment failures, see below) | Re-run by the gate itself. Backend full lane and frontend suites compared **against a clean `HEAD` worktree** to separate wave regressions from environment noise — result: **zero regressions, +1 net passing test**. See the two tables below. |

## Verification commands run by the gate

| Command | Result |
|---|---|
| `cd backend && ruff check app tests` | **331 errors — identical to clean `HEAD` baseline (331)**. Zero new lint errors from this wave. Only 1 error falls in a wave-touched file: `F401 model_validator` in `app/schemas/anthropometry.py`, **proven pre-existing** (same error on `git show HEAD:` copy; the line is outside the wave's diff hunk). |
| `cd backend && .venv/bin/python -m pytest tests/routers/test_anthropometry_bmi.py tests/test_growth_service.py -q` | **PASS — 84 passed** in 0.15 s |
| `cd backend && .venv/bin/alembic heads` | **PASS — `2a8baa967cc6 (head)`, single head, is the new revision** |
| `cd frontend && npm run typecheck` | **PASS — `tsc --noEmit` clean, 0 errors** |
| `cd frontend && npx vitest run src/lib src/hooks/athletes src/components/athletes src/routes/parents src/components/dashboard` | **1261 passed / 1262** — 92 files. The 1 failure is `src/lib/__tests__/datetime.test.ts` (pre-existing, see below). |
| `cd backend && .venv/bin/python -m pytest -q` (T015 offline lane, no overrides) | 225 failed / 3641 passed — **all 225 are environment failures** (`mysql+aiomysql://…@mysql:3306` — docker-network-only hostname unresolvable from the host shell; plus WeasyPrint native libs). |
| `cd backend && MYSQL_HOST=127.0.0.1 DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib .venv/bin/python -m pytest -q` | **169 failed / 3697 passed** |
| **Baseline differential** — same command in a clean `git worktree` at `HEAD` (`84c8061`, no wave changes) | **169 failed / 3696 passed** → **identical failure set; the wave adds exactly +1 passing test** (T013's `test_growth_source_field_present_and_null_for_legacy_rows`). **Zero regressions.** |
| Baseline differential — `ruff check app tests` at clean `HEAD` | **331 errors** → identical. Zero new lint errors. |
| Baseline differential — `npx vitest run src/lib/__tests__/datetime.test.ts` at clean `HEAD` | **1 failed / 42 passed** → identical. Confirms the frontend failure is pre-existing. |
| `git status --short` — ownership audit | **PASS** — every changed file is on a worker's ownership list. 4 files outside it (`.specify/feature.json`, `CLAUDE.md` SPECKIT block, `docs/README.md` row 18, `frontend/package.json` `check:chunks`) are **Phase 1 / Spec Kit planning artifacts**, already recorded in `checklists/setup.md` note 4. `frontend/src/components/athletes/growth/` still contains only the T002 skeleton (`index.ts` + `__tests__/.gitkeep`) — no scope creep into later waves. |
| Privacy spot-check on new fixtures | **PASS** — `MyAthleteDetailPage.growth.test.tsx` uses `"Atleta" / "Ficticio"` + a synthetic `birth_date`; the characterization test obfuscates dates to month-year and asserts the export name carries no identifying text. No real minor data in any new file. |
| Token spot-check on new frontend files | **PASS** — no raw hex in `bands.ts`, `measurementStatus.ts`, `anthropometry.schema.ts` or the new page/hook tests. The hex literals in `PercentileCurves.characterization.test.tsx` are **synthetic recharts legend-payload fixtures mirroring the legacy component's existing colours** (`#16a34a`/`#ca8a04`/`#dc2626` verified present at `PercentileCurves.tsx` L342–399) — pinned test data for a component deleted in T052, not new styling. |

## Pre-existing failures (not attributable to this wave)

All three were reproduced on a clean `git worktree` at `HEAD` with the wave's changes absent:

1. **Backend, 169 failures with overrides / 225 without** — `test_training_session_router.py`, `test_athletes.py`, `test_users.py`, `test_onboarding_consent.py`, `test_consent_endpoints.py`, `test_clubs.py` and others resolve `MYSQL_HOST=mysql` (a docker-network-only hostname) from the host shell, and depend on dev seed/login data. Identical count at `HEAD`. This is a characteristic of this machine's setup, not a defect. **Not a wave failure.**
2. **Backend, `tests/services/notification/test_stage_log_pdf.py` (13)** — WeasyPrint native libs; needs `DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib`. **Not a wave failure.**
3. **Frontend, `src/lib/__tests__/datetime.test.ts` (1)** — `currentSeason` timezone/fake-timer boundary test; its own control assertion (`expect(new Date().getFullYear()).toBe(2027)`) fails, so `vi.setSystemTime` is not taking effect in this environment. Neither `src/lib/datetime.ts` nor its test appears in `git status` — byte-identical to `HEAD`, and fails identically there. **Not a wave failure.**

## Deferred with accepted reason

- **T010 — Playwright execution.** The `history.spec.ts` edit is verified by diff review and `npm run typecheck`; running `frontend/e2e/*` requires the dev stack (`docker compose up`). Deferred to **T072** (Phase 7 full frontend gate), which already re-runs `e2e/history.spec.ts` after T052 renames the test id to `growth-curve`. Accepted: no Phase 2 code path depends on it.
- **T015 — "all green" on the full offline lane.** Read as "no regression versus baseline", which is proven by the worktree differential (169/169 identical, +1 passing). The absolute-green reading is unachievable on this machine without the docker network and the WeasyPrint libs, and is unrelated to feature 040.

## Notes for the next wave (Phase 3 — US1, `data-platform-lead`)

1. **`growth_source` round-trips as `null` today** — by design. `create_anthropometry` still writes CDC-derived values with no source tag; **T022** switches it to `GrowthSource.WHO` and persists the tag, **T023** backfills historical rows. Do not treat the current `null` as a bug.
2. **The migration is applied on the local docker MySQL** (left at head `2a8baa967cc6`). Hostinger prod is untouched — coordinate the DDL with `release-manager` before T027/T078.
3. **`bands.ts` now has two surfaces**: `getBandVocabulary(indicator, status)` (new, `NutritionalStatus`-keyed) and `getBandSpec(indicator, band)` (legacy `{label, color, narrative}`). `PercentileCurves.tsx` still carries its **own duplicate local 5-band classifier** — T026 must delete it rather than adapt it, otherwise the D2 fix is only half-applied.
4. **`weight_for_age` has no enum of its own**: it reuses the height `NutritionalStatus` keys with a text override map (`WEIGHT_LABEL_OVERRIDES`). T024/T025 must go through `getBandVocabulary`, not `BAND_VOCABULARY` directly, or the labels will read "Talla baja" for a weight indicator.
5. **T011 is the safety net for the Phase 5 deletion.** Do not weaken or delete `PercentileCurves.characterization.test.tsx` before T052 re-homes its assertions into the new component tests.
6. **`lib/measurementStatus.ts` is ready for T039** (`NextMeasurementCard`) — use `getMeasurementStatusMeta`, do not re-declare the table.
7. Pre-existing lint debt to leave alone: `ruff check app tests` is at 331 errors repo-wide (220 × F401, 76 × E402, 28 × F841). Out of scope for feature 040; do not bundle a cleanup into this branch.

**Signed**: engineering-lead (wave gate) — 2026-09-04. Phase 2 status: **pass** (T004–T015 verified complete; T010's Playwright run and T015's absolute-green reading deferred with the reasons above). Checkpoint met: defects **G-02, G-03, G-04 closed with regression tests**; `growth_source` column available; characterization tests pinned. Phase 3 may start.
