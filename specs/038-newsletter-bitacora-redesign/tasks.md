---
description: "Task list for feature 038 — Bitácora de etapa (family newsletter redesign)"
---

# Tasks: Bitácora de etapa

**Input**: `spec.md`, `plan.md`, `data-model.md`, `contracts/api.md`, `research.md`. **Tests**: mandatory (Constitution II). Every backend task ships pytest (offline lane, aiosqlite); every frontend task ships vitest (+ jest-axe on page- and dialog-level components). No real minor data in fixtures.
**Precondition**: 037 committed on `main`; branch `feat/038-bitacora`.

## Format: `[ID] [P?] [Story] Description`

## Wave 1 — content model & data (parallel, disjoint files)

- [ ] T101 [P] [US1,US2] `app/services/training/stage_log.py` (models, `BlockState`, `BADGE_LABELS`, `to_parent_dto`) + `stage_log_builder.py` (`build_stage_log`, `trail_waypoints`, `effort_profile`, `summit`, `next_segment`, `stage_number`) + `newsletter_builder.py` additions (`pdf_only_blocks.weekly`, `next_focus_groups`). Tests: waypoint priority/caps (race > badge > streak…, next race last, ≤ 6), no-race month → training summit, zero-attendance month, weekly profile boundaries (ISO weeks across month edges), `to_parent_dto` exact key set (allow-list), badge labels never codes.
- [ ] T102 [P] [US4,US6,US7] Migration `newsletter_stage_log` (`down_revision="f7a8b9c0d1e3"`): columns on `athlete_monthly_newsletters` + table `newsletter_delivery_events` (enum with `values_callable`). Model updates; `AthleteNewsletterRead` / `AthleteNewsletterPatch` extensions; PATCH re-derives `stage_log_json`, resets `pdf_sha256`, validates `selected_race_insight_ids` permutation and `coach_note` (≤ 60 words, redaction). Tests: patch persists overrides/hidden/coach_note, permutation rejected (422), approved → draft on edit, denied path (parent 403), v1 row untouched by PATCH of v1 fields.
- [ ] T103 [P] [US3] `app/services/training/family_translation.py`: `filter_for_family(InsightV3)` (drop list per AC-3.2, returns `None` when no eligible action) + `select_insight(db, newsletter, consent)` (approved, active, `structured_json`, event date in month, first in `selected_race_insight_ids`). Tests: field-domain observations dropped, low-priority actions dropped, no consent → None, event outside month → None, order respected.
- [ ] T104 [P] [US2] `newsletter_static_copy.py` v2 functions (title, observations, summit caption, next segment, family compass), gender-aware via `athlete_reference`, data-driven. Tests: each function on full / no-race / zero-attendance snapshots; never mentions supplements or calories; question ends with `?`.

## Wave 2 — AI v2 + delivery (parallel)

- [ ] T201 [US2,US3] `prompts/athlete_monthly_newsletter_v2.j2` (method + rules + data blocks + worked example) + registry entry; `use_cases/athlete_monthly_newsletter_v2.py` (`StageNarrative`, structured output with JSON-repair retry, `regenerate_block`, `StageNarrativeGuardrails`: v1 rules + grounding via `extract_numeric_tokens` + forbidden phrases + overlap vs previous month + `?` check); generation path writes v2 when `settings.newsletter_content_version == 2`; `POST /{id}/regenerate-block`; `POST /{id}/convert`. Tests with `FakeLLMProvider` returning JSON: happy path, grounding violation → static block + `grounding_violations`, forbidden phrase → block rejected, `only_block` returns one key, 451 without consent, 409 on `sent`, convert on v1 draft only.
- [ ] T202 [P] [US1,US6] `routers/parent_newsletters.py` (list / detail / pdf / read) + `schemas/parent_newsletter.py`; `GET /my-athletes` gains `unread_newsletters`; mount in `main.py`. Tests: parent sees only linked athletes (404 otherwise), only `sent`, coach → 403, read idempotent + `web_read` event, parent DTO has no `source_insight_id` / anthropometry keys, unread count.
- [ ] T203 [P] [US5] `templates/email/athlete_stage_log.html`, `templates/documents/pdf/athlete_stage_log.html`, `_trail.svg.j2`; `template_registry.py`; `athlete_newsletter_pdf.py::generate_stage_log_pdf` with the annex rule; `newsletter_dispatcher.py` picks v2 by `content_version`, deep link vs "Activa tu cuenta", writes `sent` events with `provider_message_id`; `GET /{id}/render?surface=email` (sandbox CSP). Tests: email size ≤ 100 KB and no anthropometry strings, PDF page count ≤ 3 (pypdf) on the three fixtures, annex only when a record is dated in the month, v1 rows still use v1 templates, sent events written (SMTP → null id).
- [ ] T204 [P] Frontend contract: `types/stageLog.types.ts`, `schemas/stageLog.ts` (Zod), `api/athleteNewsletters.ts` (+regenerate/convert/render), `api/parentNewsletters.ts`, hooks (`useRegenerateBlock`, `useUpdateStageLog`, `useParentNewsletters`, `useParentNewsletter`, `useMarkNewsletterRead`), MSW handlers, `fixtures/stageLog.ts` (full / no-race / zero-attendance), `applyOverrides()` pure helper. Tests: Zod parses fixtures, hooks invalidate the right keys, `applyOverrides` merges and clears.

## Wave 3 — UI (parallel)

- [ ] T301 [US1,US5] `components/newsletter/StageLogView.tsx` + blocks (`StageHeader`, `TrailRoute` SVG with hidden `<ol>`, `SummitCard`, `ObservationsList`, `AnalystReading`, `EffortProfile` on recharts per the dataviz skill, `NextSegment`, `FamilyCompass`, `BadgesRow`, `PhotosGrid`, `CoachNote`), `mode` prop, `data-block` anchors, `data-surface="bitacora"` styles (topographic pattern, dashed trail; `--color-trail-earth` behind a token so it can be dropped). `ux-researcher` review of the earth tone and the 360 px layout. Tests: every block renders / hides on the three fixtures, no placeholder text in parent mode, jest-axe zero violations, trail list matches waypoints.
- [ ] T302 [US4,US6] `routes/training/AthleteNewsletterStudioPage.tsx` + `components/newsletter/studio/*` (`DevicePreview` with Móvil / Correo / PDF toggle, `EmailPreviewFrame` sandboxed iframe, `BlockPanel`, `BlockCard` with state badge + word counter + inline edit + hide, `RegenerateDialog`, `AnalystPicker`, `DeliveryPanel` masked emails, `StatusStepper`); route switch by `content_version`; "Convertir a bitácora" on v1 drafts. Tests: edit updates preview optimistically then PATCH, regenerate calls the endpoint with instruction, hide toggles `hidden_blocks`, picker PATCHes a permutation, stepper states incl. Leído, a11y zero violations, tablet layout (two columns) vs narrow (tabs).
- [ ] T303 [P] [US1] Parent: `routes/parents/newsletters/ParentNewsletterListPage.tsx`, `ParentNewsletterPage.tsx` (read receipt once per session, "Descargar PDF"), nav entry "Bitácora" in `ParentSidebar` + `ParentBottomNav`, `ChildCard` "Nueva bitácora" chip, routes in `App.tsx`. Tests: receipt fired once, chip hidden after read, cold-start "starting server" state preserved, a11y zero violations.

## Wave 4 — quality

- [ ] T401 [US6] (P3) `routers/webhooks_resend.py`: Svix verification, idempotency, event mapping, 404 without secret; `config.resend_webhook_secret`; `DeliveryPanel` shows delivered / opened / bounced. Tests: valid signature, bad signature 400, replay ignored, unknown id ignored, no PII in logs.
- [ ] T402 Privacy audit (`data-privacy-guard`): prompts v2, parent DTOs, email/PDF v2, delivery events, logs. Fix findings.
- [ ] T403 Perf + a11y + e2e: Playwright flow coach studio → send → parent reads (MSW-free, local backend); LCP check on the parent page under Fast 3G; email in MailHog + Gmail Android checklist; PDF visual check on the three fixtures.
- [ ] T404 Docs: `docs/06-parents/038-bitacora.md`, `docs/implementation-status.md`, `docs/technical-notes.md` (dated entry), CLAUDE.md "most recent feature" block; `.env.example` entries `NEWSLETTER_CONTENT_VERSION`, `RESEND_WEBHOOK_SECRET`.
- [ ] T405 Verification: `pytest`, `ruff check`, `npm run typecheck`, `npm test`; SC-1 regeneration of June 2026 for the screenshot athlete + 5 others on the local DB (aggregate report only, no names).
