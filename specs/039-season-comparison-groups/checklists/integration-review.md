# Integration review — feature 039 (season comparison groups)

**Reviewer**: engineering-lead · **Date**: 2026-09-03 · **Branch**: `feat/039-season-comparison-groups` (uncommitted working tree)
**Scope**: `git diff main` — 44 files, +4028/−236 (backend services/routers/schemas/templates, race AI pipeline, frontend `athletes/ai` surface, tests, fixtures, docs, spec dir).
**Inputs**: `.specify/memory/constitution.md` v1.2.0 · `contracts/{evolution-api,newsletter-context,ai-context}.md` · `spec.md` FR-001..FR-022 · `research.md` D1–D13 · `checklists/privacy-audit.md` (T045) · `checklists/quickstart-run.md` (T048) · T046/T047 reports.

Verdicts: **PASS** = verified in code + covered by a test · **PARTIAL** = implemented but deviates from the literal requirement · **OPEN** = not verifiable yet.

## A. Explicit verification points

| # | Item | Verdict | Evidence (file:line) | Notes |
|---|---|---|---|---|
| a1 | Newsletter cup loop reads one group per block | PASS | `backend/app/services/training/newsletter_builder.py:709-727`; `backend/templates/documents/pdf/athlete_stage_log.html:383-401` | `split_progression` groups by `series_id`; the template iterates `charts_annex.cups[]` and each block only sees `cup.positions/gap_pcts/points_accumulated`. Covered by `test_newsletter_builder.py::test_charts_context_per_cup_points_accumulated_isolated`. |
| a2 | `EvolutionChart` selector renders a single group | PASS | `frontend/src/components/athletes/ai/EvolutionChart.tsx:88-136, 254-268` | Server-side filter when `series_id` is sent; client-side filter by `activeGroupId` on the unfiltered first render. Fallback to the full series only when the filter matches nothing (legacy points without `series_id`). |
| a3 | `MiniSparkline` draws only the first cup | PASS | `frontend/src/components/athletes/ai/MiniSparkline.tsx:53-88, 104-116` | Filter active when `groups.length > 1`; empty state `Sin válidas de copa en esta temporada.` when the season has groups but no cup. Covered by `MiniSparkline.test.tsx` (T020, 5 cup points, no `CD`/`CN`). |
| a4 | AI receives cup/championship split | PARTIAL | `backend/app/services/race/ai/nodes/compute_metrics.py:303-329, 412` | `metrics.progression_groups` is produced exactly as `contracts/ai-context.md` specifies, but **no consumer reads it** — the model actually sees the separation through the new `serie` column of `_progression_to_md` and `series_label_v3` in the season table. Matches the contract; see F-4 for the FR-012 wording. |
| b | Accumulated points per cup | PASS | `newsletter_builder.py:1394-1418` (`acc` resets per cup) | `test_newsletter_builder.py:766` asserts the last `points_accumulated` point equals `standings.get_event_standings` cup total; `:846` asserts two cups stay isolated. Championships never enter `cups[]`, so they never touch the points axis (FR-003). |
| c | Season section independent of the anthropometry annex | PASS | `athlete_stage_log.html:352-357` (annex closes), `:359-380` (own `page-break-before` + own `{% if %}`) | `test_newsletter_pdf_groups.py::TestStageLogPdfSeasonSectionIsIndependentOfAnthro` renders with and without `anthro_annex` and asserts ordering. Cup charts remain gated by `has_race_this_month` (`athlete_newsletter_pdf.py:141-146`) — pre-existing behavior, consistent with the spec's "as the charts already do today"; the `Campeonatos` block is **not** gated. |
| d1 | AI race resolution by `event_id` | PASS | `compute_metrics.py:127-162`; `ai/nodes/analyst_agent.py:118-172, 512-533`; `load_race_data.py:121-162` | Anchor first, `valida_num` only among `series_kind == "cup"` rows; `_field_metrics_for_row` looks up the pelotón by the resolved row's `event_id`. Covered by `test_analyst_agent_resolution.py` and `test_graph_championship.py:137` (CN anchored, `race_row["event_id"] == CN_EVENT_ID`). |
| d2 | Same-series season comparative | PASS | `compute_metrics.py:217-241` | Priors = same `series_id` + earlier `event_date`; a championship returns `([], first_reference)`. Covered by `tests/services/test_compute_metrics_season.py`. |
| e | Level-aware labels everywhere | PASS (1 residual) | PDF: `newsletter_builder.py:770-780` (`build_race_label` + `_race_readable_label`) · chart dot: `EvolutionChart.tsx:434, 452` · `validaLabel`: `frontend/src/lib/insights.ts:196-201` (+ `AthleteAIAnalysisTab.tsx:421`, `HeroLastInsightCard.tsx:119`, `InsightsTimeline.tsx:258-266`) · `series_label_v3`: `agents/analyst.py:508-520` · `recall_memory.py:157-172` (already level-aware) · API: `schemas/athlete_race_analysis.py:161` (`series_level` on `AthleteInsightOut`, eager-loaded at `insights_history.py:192`) | Residual F-3: `MiniSparkline.romanForValida` still hardcodes `"CD"` for any championship; unreachable in the normal multi-group path because championships are filtered out first. |
| f1 | Old newsletter snapshots | PASS (documented behavior) | `newsletter_builder.py:1376-1378` + `:1426` (legacy builder kept); `athlete_stage_log.html:378-380` | No error, no partial render: a pre-039 snapshot yields `season_cups = []` and simply drops the season page. Asserted by `test_newsletter_pdf_groups.py::test_old_snapshot_without_anthro_has_no_season_page`. Consequence worth stating in the PR: **re-downloading an old newsletter no longer shows its season charts** (see F-5). |
| f2 | `GET /evolution` without `series_id` | PASS | `analytics_charts.py:441-459`; `routers/athlete_race_analysis.py:990-1016` | `series_id` optional, `groups` always populated, `selected_group=None`, every pre-existing field kept. Unknown `series_id` → `series=[]` + 200 (`test_analytics_charts.py:655`); `series_id=0` → 422 (`test_..._evolution_groups.py:397`); parent cross-athlete denial identical with the filter (`:358`). |
| g | No new N+1 | PASS | `analytics_charts.py:227-291` (single CTE, one `db.execute`; `groups` derived in Python — research D4) · `newsletter_builder.py:749-752` (4 `load_*` calls hoisted **outside** the competitor loop; `compute_field_metrics` is pure in-memory) · `:640` (`_lookup_category_labels` batched once for month + season codes) | No query inside any loop. Cost added to the newsletter is 4 table loads, only when the athlete raced a championship, on the pre-existing `queries.load_*` pattern already used by `athlete_progression`. |
| h | Copy in español neutro with diacritics | PASS | `athlete_stage_log.html:385, 410, 415`; `charts/championship_card.html.jinja:32-57`; `EvolutionChart.tsx:138-143, 208`; `ChampionshipReadingCard.tsx:64-67, 100-103`; `MiniSparkline.tsx:109-111` | All D13 strings present verbatim and accented: `Evolución en la {copa}`, `Campeonatos`, `Posición`/`Pelotón`/`Gap al P1`/`Percentil`, `No completó la prueba.`, the separation note, `Competencia` (sr-only), `Tendencia a lo largo de las válidas de {copa}.`, `Resultado frente a su propio pelotón.`, `Sin válidas de copa en esta temporada.` No clinical or judgmental wording about the minor. |

## B. Constitution (v1.2.0)

| Principle | Verdict | Evidence / notes |
|---|---|---|
| I — Code quality | PASS | `ruff` clean on the 28 touched Python files (T047; the 7 remaining findings are byte-identical to `main`, and the branch nets 2 fewer repo-wide errors than the pre-039 baseline). `tsc --noEmit` clean. New public modules carry docstrings (`comparison_groups.py:1-27`, `evolutionFormat.ts:1-12`, `ChampionshipReadingCard.tsx:1-22`, the Jinja macro header). No duplication introduced beyond the deliberate second grouping implementation in `analytics_charts._build_comparison_groups` (raw SQL rows vs. `athlete_progression` rows), justified in its docstring at `:89-97`. Human review still required before merge (I-4). |
| II — Testing (NON-NEGOTIABLE) | PASS, with the golden gate OPEN | 186 backend tests across the 12 feature files pass (`.venv/bin/python -m pytest`), 131 frontend tests across the 6 touched specs pass, `jest-axe` zero violations with the new selector present (`a11y.v2.test.tsx`). Denied-path coverage for the new query param (`test_..._evolution_groups.py:358`). Privacy invariants asserted (`test_athlete_race_analysis_privacy.py`). Remaining flake is the pre-existing TZ-dependent `src/lib/__tests__/datetime.test.ts`, unrelated to 039. **T040 (`pytest -m golden`, threshold 0.75) is neither passed nor failed — see B-1.** |
| III — UX consistency | PASS, 2 notes | Copy policy respected on both sides (product copy español neutro / `docs/**` in English, see `docs/06-parents/003-newsletter-improvements.md`). New control reuses the exact classes of the two existing selects (research D6). Loading / empty / error states defined for the championship view (`EvolutionChart.tsx:254-273`) and the sparkline (`MiniSparkline.tsx:104-116`). Notes: F-1 (a stat tile shows `—` where the spec expects a number) and F-6 (select height ~36 px, below the 48 px touch floor — parity with the two pre-existing selects, not a regression). |
| IV — Performance | PASS | One SQL round-trip for `/evolution` including the group list (research D4); no N+1 (item g); no new runtime dependency; no new lazy-route weight (`ChampionshipReadingCard` is a small leaf under the already-lazy Insights surface); `evolutionFormat.ts` extraction is net-neutral. |
| V — Youth psychological assessment | N/A | Feature touches no psychological instrument. |
| Quality gates — Ley 1581 | PASS | T045 privacy audit: 0 critical / 0 high / 0 medium; the only notes are pre-existing fixtures untouched by 039. Championship readings expose only the athlete's own position/field size/gap/percentile — no third-party identity (`newsletter_builder.py:770-790`, `schemas/athlete_race_analysis.py:441-470`). Prompt corpus uses `{{ athlete_ref }}`; golden `case_009.json` carries no name. |
| Workflow | PASS | Branch `feat/039-season-comparison-groups` per `<type>/<slug>`; commit message and PR description carry no AI-tooling mention; `tasks.md` untouched by this review. |

## C. Contracts

| Contract | Verdict | Notes |
|---|---|---|
| `evolution-api.md` | PASS, 1 deviation | Request/response shape, ordering, back-compat, error codes and the frontend client signature all match (`schemas/athlete_race_analysis.py:410-531`, `api/athleteRaceAnalysis.ts:213-229`, `hooks/athletes/useAthleteEvolution.ts:18-31`). `RaceParticipationOption` gained the three fields (FR-017 picker untouched). Deviation: `confidence` is only per-group once a group is explicitly selected — see F-2. |
| `newsletter-context.md` | PASS | `race_results.cups[]/championships[]`, `progression_history` kept one release, `charts_context.cups[]` with `n_samples`/`low_confidence`, dedupe by `event_id` (`newsletter_builder.py:611-620`, `:757-766`), card partial with the four tiles + not-finished state, template `break-inside: avoid` per block, email template untouched. |
| `ai-context.md` | PASS, 1 nit | State keys, the three resolution rules, the `serie` column, prompt rule 10 (`prompts/race_analyst_v3.md:50`), rollback to `race_analyst_v2` intact, `case_009.json` added. Nit F-7: `progression_groups["cups"]` is keyed by `int` while the contract writes `"<series_id>"` (strings); harmless once the state is JSON-serialized into the checkpointer, but the contract and the test (`test_compute_metrics.py:112`) disagree on paper. |

## D. Findings

Severity: **M** = fix or waive before merge · **L** = follow-up.

| ID | Sev | Finding | Evidence | Suggested action |
|---|---|---|---|---|
| F-1 | M | **FR-008 partially met.** The championship card in the athlete detail can only fill two of the four required readings at a time: `Posición` is rendered only when the active metric is `ranking` and `Gap al P1` only when it is `podium_gap_ms` (the default). `EvolutionPoint` carries no raw `position`/`gap_pct`, so with the default metric the coach sees `Posición —`. The newsletter card (which reads `field_metrics`) shows all four. | `frontend/src/components/athletes/ai/ChampionshipReadingCard.tsx:44-61` (documented as deliberate at `:10-21`) | Either add `position` and `gap_pct` to `EvolutionPoint` (both already available in the CTE: `ar.position`, and `gap_pct` derivable from `winner_time_ms`) and render the four tiles unconditionally, or record a product waiver for the reduced reading. Not fixable as a trivial edit — needs schema + service + tests. |
| F-2 | L | **FR-009 edge case.** On first render no `series_id` is sent, so `confidence` is computed over the whole season while the chart already shows only the default cup. Divergence only when the counts straddle a threshold (e.g. 7 cup rounds + 2 championships → season `high`, cup alone `medium`). Corrects itself as soon as any group is picked. | `analytics_charts.py:441-455`; `EvolutionChart.tsx:77-136, 371` | One-line frontend fix (derive the notice from `displaySeries` instead of `query.data.confidence`), or send `series_id` once `defaultGroupId` resolves. |
| F-3 | L | **Residual level label.** `romanForValida` returns `"CD"` for any championship regardless of level; reachable only when the season yields ≤ 1 group (the group filter is gated on `groups.length > 1`), where a lone national championship would tooltip as `V-CD`. | `frontend/src/components/athletes/ai/MiniSparkline.tsx:43-47, 62-67` | Pass `series_level` and return `"CN"` for `national`, or drop championship points unconditionally. |
| F-4 | L | **FR-012 wording vs. implementation.** The AI receives one flat progression table with a `serie` column, not two separate inputs; `metrics.progression_groups` is produced but never consumed. Consistent with `contracts/ai-context.md`, not with the literal FR-012 ("separate inputs"). | `compute_metrics.py:412`; `agents/analyst.py:301-350` | Either wire `progression_groups` into the v3 prompt in a follow-up, or reword FR-012 in the spec to match the contract. |
| F-5 | L | **Historical newsletters lose their season charts.** Snapshots persisted before 039 keep the flat `positions`/`gap_pcts`/`points_accumulated` keys, which the new template no longer reads; re-downloading such a newsletter silently drops the "Evolución en la temporada" page (no error — contract satisfied) and rewrites `pdf_sha256`. | `athlete_stage_log.html:378-380`; `test_newsletter_pdf_groups.py:339`; `newsletter_builder.py:1426` (legacy builder now only reachable from non-PDF callers) | Product call: accept (recommended — the old chart is the misleading mixed one this feature removes) or add a legacy fallback block in the template. Must be stated in the PR/release notes. |
| F-6 | L | New `Competencia` select is ~36 px tall (`px-3 py-2 text-sm`), below the constitution's 48 px touch floor — identical to the two pre-existing selects it sits next to (research D6 chose parity on purpose). | `EvolutionChart.tsx:208-217` | Fold into the future shadcn `Select` migration (feature 033 territory), not into 039. |
| F-7 | L | `progression_groups["cups"]` keyed by `int` vs. the contract's `"<series_id>"` string keys. | `compute_metrics.py:324-327`; `contracts/ai-context.md:10` | Align the contract text (cheapest) or cast the keys. |
| F-8 | L | `field_size` in `/evolution` counts `status='finished' AND race_time_ms IS NOT NULL`, while `compute_field_metrics` (the number the newsletter card and the AI cite) counts all `FINISHED` rows. Research D3 asks for "the same criterion", and the code comment claims it. Diverges only for a finished result with no recorded time. | `analytics_charts.py:255-267, 396-410` vs. `field_metrics.py:84-89` | Follow-up: drop the `race_time_ms IS NOT NULL` condition from the `cat_size` count (keeping it for MIN/MAX), or correct the comment and D3. |
| F-9 | L | `docs/06-parents/003-newsletter-improvements.md` states the season section "renders whenever there is a cup round or a championship to show"; the cup charts are still gated by `has_race_this_month` (only the `Campeonatos` block is ungated). | `docs/06-parents/003-newsletter-improvements.md` (last bullets) vs. `athlete_newsletter_pdf.py:141-146` | One-sentence doc correction by the doc owner. |
| F-10 | L | `EvolutionPoint.series_name` / `RaceParticipationOption.series_name` declare `min_length=1` but the service substitutes `""` when the column is `NULL`, which would raise a 500 instead of degrading. `race_series.name` is non-nullable today, so unreachable. | `analytics_charts.py:392, 824`; `schemas/athlete_race_analysis.py:414-421, 656-660` | Follow-up: drop `min_length` or fall back to a placeholder. |

**Fixed during this review (trivial only):** stale header comment of `backend/templates/documents/pdf/athlete_stage_log.html` (documented `charts_annex` as "the v1 curves" and claimed the season charts live inside the growth annex; now documents `charts_annex.cups[]`, the new `race_results` variable and the independent season page). Comment-only change; `test_newsletter_pdf_groups.py` + `test_newsletter_builder.py` re-run: 46 passed.

## E. Blocking

1. **B-1 — T040 golden evaluation gate is OPEN.** Run today with the real key: case 001 = 0.840, case 002 = 0.820; then the Gemini free-tier quota was exhausted (HTTP 429 `RESOURCE_EXHAUSTED` on `gemini-3.8-flash`) and cases 003–009 fell back to the deterministic path, so they were **not measured**; the scoreboard file was restored to its pre-run version. Neither passed nor failed. FR-016 / SC-005 cannot be signed off until the quota resets and `pytest -m golden` is re-run in full at composite ≥ 0.75.
2. **B-2 — F-1 (FR-008) needs a fix or an explicit product waiver** before the athlete-detail story is declared complete: with the default metric the championship card shows `Posición —`, which reads as missing data on a P2 coach surface.

Everything else in section D is a follow-up and does not block the merge.

## F. Test totals at review time

- Backend, 12 feature files: **186 passed** (`.venv/bin/python -m pytest -q -p no:cacheprovider`, offline lane).
- Backend, whole offline lane (T047): 3628 passed / 222 failed, every failure in a file with a 0-line diff vs. `main` (MySQL host unreachable outside Docker, WeasyPrint native libs missing, 3 stale unrelated expectations).
- Frontend, 6 touched specs: **131 passed**. Whole suite (T047): 3691 passed / 1 failed (pre-existing TZ flake in `src/lib/__tests__/datetime.test.ts`).
- `ruff` on touched files: clean. `npm run typecheck`: clean. `jest-axe`: zero violations.
- `pytest -m mysql` (T042 dialect case): not exercised this pass — `TEST_DATABASE_URL` unset by design.

## G. Post-review resolution (2026-09-03)

- **B-2 / F-1 — RESOLVED.** `EvolutionPoint` now carries `position` and `gap_pct` for every metric (schema + `build_evolution` + tests), and `ChampionshipReadingCard` renders the four readings from the point regardless of the active metric (specs updated for `podium_gap_ms` and `ranking`). Contract and data-model updated.
- **F-3 — RESOLVED.** `MiniSparkline.romanForValida` is level-aware (`CD` / `CN`) via the point's `series_level`, unit-tested. (Its in-code comment references this as "Fix F-2" — a pre-existing mislabel; the finding is F-3, "Residual level label", per section D above.)
- **F-2 — RESOLVED.** `EvolutionChart`'s low-confidence notice now derives from the group actually displayed on first render — new `confidenceForPoints()` (`lib/insights.ts`) applied to `displaySeries` — instead of the season-wide `query.data.confidence`, until the user explicitly picks a `series_id`, at which point the server-filtered value is used as-is. Two new MSW fixtures cover the divergent-threshold case (`athleteRaceAnalysisHandlers.ts`) plus specs in `EvolutionChart.test.tsx`.
- **F-4 — RESOLVED, with a documented residual.** `_progression_to_md` and `_v3_season_block` (`agents/analyst.py`) now render two headed markdown tables (`**Válidas de copa**` / `**Campeonatos (pelotón propio, no comparable con la copa)**`) whenever a season mixes cup and championship rows; `_progression_to_md` additionally sub-groups cup rows by `series_id`/`series_name`. `_v3_season_block`'s rows come from `field_context`/`FieldMetrics`, which never carries `series_id` — in production that table still degrades to one ungrouped cup table (the cup-vs-championship split itself, the actual FR-012 ask, is done in both tables). New tests: `test_analyst_progression_md.py`, `test_prompt_v3_blocks.py`.
- **F-5 — RESOLVED.** `athlete_stage_log.html` renders a legacy-snapshot fallback block: a pre-039 snapshot (flat `positions`/`gap_pcts`/`points_accumulated`, no `cups`) still shows the old single "Evolución en la temporada" chart set instead of silently dropping the season page on re-download. New coverage in `test_newsletter_pdf_groups.py`.
- **F-6 — RESOLVED.** `min-h-12` added to the three native selects (`#evo-season`, `#evo-metric`, `#evo-group`) in `EvolutionChart.tsx`, meeting the 48px touch floor.
- **F-7 — RESOLVED.** `_build_progression_groups` (`compute_metrics.py`) casts `progression_groups["cups"]` keys to `str`, matching `contracts/ai-context.md`'s `"<series_id>"` notation. Test updated: `test_compute_metrics.py`.
- **F-8 — RESOLVED.** `build_evolution`'s `cat_stats` CTE counts `field_size`/percentile over every `status='finished'` row regardless of a recorded time, aligning with `field_metrics.compute_field_metrics`'s criterion; a separate `cat_size_with_time` column keeps gating the pre-existing ≥5 rule for the time-based percentile. Test updated: `test_analytics_charts.py`.
- **F-9 — RESOLVED (behavior change).** The "Campeonatos" block in `athlete_newsletter_pdf.py` now shares the same `has_race_this_month` gate as the cup charts (it was previously ungated) — `race_results` is dropped to `None` under the same condition as `charts_annex`; regression test added in `test_stage_log_pdf.py`. `docs/06-parents/003-newsletter-improvements.md` corrected to state both blocks share the gate.
- **F-10 — RESOLVED.** `EvolutionPoint.series_name` / `RaceParticipationOption.series_name` now fall back to `"Serie"` instead of `""` when the column is `NULL`, avoiding a 500 against the schema's `min_length=1`. Still unreachable today (`race_series.name` is non-nullable), but no longer a latent bug.
- **B-1 — still OPEN.** Golden gate blocked by the Gemini free-tier quota (`generate_content_free_tier_requests`, limit 20/day on `gemini-3.8-flash`); re-run `pytest -m golden` after the daily reset.

- **Closure round 2 (2026-09-04 UTC) — page-count test.** `tests/services/notification/test_stage_log_pdf.py::test_full_month_with_annex_still_within_three_pages` renders 4 pages on this macOS environment **both on `main` and on the feature branch** (verified in a temporary worktree of `main`; the coach-note block already spills to a third page before the growth annex — font metrics differ from CI). Not a 039 regression. The season section no longer forces `page-break-before: always`; it flows after the growth annex (or after page 2) with `break-inside: avoid` per block, matching the 038 layout intent.

- **B-1 — RESOLVED with caveat (2026-09-04 13:42 UTC).** `pytest -m golden` (v3, 9 cases) average **0.821 ≥ 0.75**, case 009 (national championship) **0.850** with `forbidden = 1.00`; lowest case 007 = 0.680 (pre-existing forbidden-term miss unrelated to rule 10). The analyst ran on `gemini-3.1-flash-lite` (`RACE_AI_ANALYST_MODEL` override) because the free-tier daily quota of `gemini-3.8-flash` (20 requests, shared with production) was exhausted; with `gemini-3.8-flash` cases 001/002 had scored 0.840/0.820 on 2026-09-03. A CI run on the production model still needs quota headroom (or billing).
